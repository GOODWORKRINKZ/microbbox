# Список изменений МикРоББокс 0.1

## 🔧 Патч от 2025-11-06 - Исправление режима следования по линии

### Исправлены критические баги

**Проблема**: После включения робот начинал крутиться против часовой стрелки из-за автоматической активации режима следования по линии и неприменения настроек моторов.

**Исправления**:

1. ✅ **Режим следования теперь использует единую логику управления моторами** (`src/LinerRobot.cpp`)
   - Функция `applyPIDControl()` переработана для использования PWM сигналов (1000-2000)
   - Вызывается `setMotorPWM()` вместо прямого вызова `setSpeed()`
   - **Устранено дублирование кода** - все настройки применяются в одном месте
   - Автоматически применяются инверсия и своп моторов из WiFiSettings
   - Гарантируется одинаковое поведение в ручном и автономном режимах

2. ✅ **Предотвращение ложных срабатываний кнопки** (`include/hardware_config.h`)
   - Увеличено время антидребезга с 50 до 200 мс
   - Добавлена задержка 2 секунды перед первой проверкой кнопки после инициализации
   - Предотвращает ложную активацию режима следования при загрузке

3. ✅ **Улучшенная диагностика**
   - Добавлены отладочные сообщения о PWM сигналах в режиме следования
   - Логирование значений throttle и steering в формате PWM

### Технические детали

- **Файлы изменены**:
  - `src/LinerRobot.cpp` - функция `applyPIDControl()` и `initButton()`
  - `include/hardware_config.h` - константы `BUTTON_DEBOUNCE_MS` и `BUTTON_INIT_DELAY_MS`

- **Алгоритм управления в режиме следования**:
  1. Расчет PID управления на основе позиции линии
  2. Преобразование baseSpeed (0-100%) в throttlePWM (1500-2000)
  3. Преобразование steering (-100 до +100) в steeringPWM (1000-2000)
  4. Вызов `setMotorPWM()` который автоматически применяет все настройки:
     - Преобразование PWM в дифференциальное управление
     - Инверсия левого мотора (если включена)
     - Инверсия правого мотора (если включена)
     - Своп моторов (если включен)

- **Преимущества нового подхода**:
  - Единая точка применения настроек моторов
  - Отсутствие дублирования логики
  - Легче поддерживать и тестировать
  - Гарантированная консистентность между режимами

---

## 🎉 Главные изменения

### Модульная архитектура с SOLID принципами

Проект полностью переработан с применением лучших практик разработки ПО:

- ✅ **Single Responsibility** - каждый класс имеет одну ответственность
- ✅ **Open/Closed** - легко расширяется без изменения существующего кода
- ✅ **Liskov Substitution** - любой тип робота заменяет базовый класс
- ✅ **Interface Segregation** - маленькие, сфокусированные интерфейсы
- ✅ **Dependency Inversion** - зависимость от абстракций

### Три типа роботов из одной кодовой базы

1. **МикроБокс Классик** - управляемый робот (как раньше)
2. **МикроБокс Лайнер** - автономный робот следующий по линии
3. **МикроБокс Брейн** - модуль управления для других роботов

### Система конфигурации в стиле ExpressLRS

- ✅ Чистая конфигурация через `platformio.ini`
- ✅ Нет препроцессорных директив в коде
- ✅ Автоматический выбор компонентов для каждого типа
- ✅ 6 готовых окружений сборки (classic/liner/brain × debug/release)

## 📁 Новые файлы

### Конфигурация
- `include/target_config.h` - конфигурация типов роботов
- `include/hardware_config.h` - конфигурация пинов и железа

### Интерфейсы
- `include/IComponent.h` - базовый интерфейс компонента
- `include/IRobot.h` - интерфейс робота
- `include/IMotorController.h` - интерфейс контроллера моторов

### Базовые классы
- `include/BaseRobot.h` / `src/BaseRobot.cpp` - общая функциональность всех роботов

### Реализации роботов
- `include/ClassicRobot.h` / `src/ClassicRobot.cpp` - управляемый робот
- `include/LinerRobot.h` / `src/LinerRobot.cpp` - робот следующий по линии
- `include/BrainRobot.h` / `src/BrainRobot.cpp` - модуль управления

### Контроллеры
- `include/MX1508MotorController.h` / `src/MX1508MotorController.cpp` - контроллер моторов

### Документация
- `ARCHITECTURE.md` - подробная документация архитектуры

## 🗑️ Удаленные/переименованные файлы

- `include/config.h` → `include/target_config.h` + `include/hardware_config.h`
- `include/MicroBoxRobot.h` → разделен на `BaseRobot.h` + конкретные типы роботов
- `src/MicroBoxRobot.cpp` → разделен на `BaseRobot.cpp` + конкретные типы роботов

## 🔧 Измененные файлы

### platformio.ini
- Добавлены новые окружения сборки:
  - `classic-debug` / `classic-release`
  - `liner-debug` / `liner-release`
  - `brain-debug` / `brain-release`
- Старые окружения `debug` / `release` теперь указывают на `classic-*` (обратная совместимость)

### main.cpp
- Добавлен фабричный паттерн для создания нужного типа робота
- Автоматический выбор типа на основе `TARGET_*` define

### README.md
- Обновлена документация с описанием новой архитектуры
- Добавлено описание трех типов роботов
- Обновлены инструкции по сборке

### WiFiSettings.cpp, FirmwareUpdate.h
- Обновлены include-ы с `config.h` на новые файлы конфигурации

## 📊 Преимущества новой архитектуры

### Для разработчиков
- **Модульность**: легко добавлять новые типы роботов
- **Переиспользование**: общий код в базовых классах
- **Тестируемость**: каждый компонент тестируется отдельно
- **Читаемость**: четкая структура и ответственность

### Для пользователей
- **Выбор типа**: три робота из одной прошивки
- **Простота**: выбрал окружение при сборке - всё настроено
- **Расширяемость**: легко добавить свой тип робота

### Для сборки
- **Чистота**: нет директив препроцессора в коде
- **Профессионализм**: система как в ExpressLRS
- **Множественность**: 6 готовых конфигураций

## 🚀 Миграция с v1.x

### Если вы собирали обычный робот
```bash
# Было:
pio run --environment release

# Стало (или используйте старое - работает):
pio run --environment classic-release
```

### Если вы модифицировали config.h
Теперь конфигурация автоматическая через `target_config.h` и `hardware_config.h`. Для кастомизации:
1. Выберите нужный тип робота через окружение
2. При необходимости измените пины в `hardware_config.h`

### Если у вас custom код на базе MicroBoxRobot
- Функционал перенесен в `ClassicRobot`
- Общая логика в `BaseRobot`
- Используйте наследование от `BaseRobot` для своих типов

## 🎯 Что дальше?

### Планы развития
1. Реализация алгоритмов следования по линии для Liner
2. Поддержка дополнительных протоколов для Brain (PPM, SBUS, TBS)
3. Улучшение веб-интерфейса с учетом типа робота
4. Unit-тесты для компонентов
5. CI/CD для автоматической сборки всех конфигураций

### Как добавить свой тип робота
Подробная инструкция в [ARCHITECTURE.md](ARCHITECTURE.md#-добавление-нового-типа-робота)

## 📞 Поддержка

Если возникли вопросы или проблемы:
1. Прочитайте [ARCHITECTURE.md](ARCHITECTURE.md) - там много деталей
2. Проверьте примеры в документации
3. Создайте Issue в репозитории

---

**Версия:** 0.1.0  
**Дата:** 2025-11-03  
**Автор:** GOODWORKRINKZ + GitHub Copilot
