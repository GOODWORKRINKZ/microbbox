# Система восстановления видео потока

## Обзор

Реализована система автоматического восстановления видео потока при его обрыве с отображением fallback-изображения.

## Функциональность

### Мониторинг потока
- Автоматическое отслеживание состояния видео стрима
- Обнаружение обрыва соединения через обработчик `onerror`
- Защита от множественных срабатываний (debounce 1 секунда)

### Автоматическое переподключение
- До 10 попыток переподключения
- Экспоненциальная задержка: 2с, 4с, 6с, 8с... максимум 30с
- Информирование пользователя о текущей попытке

### Fallback оверлей
- Полупрозрачный оверлей поверх видео контейнера
- Иконка камеры 📹
- Информационное сообщение
- Анимированный спиннер загрузки
- Автоматическое скрытие при восстановлении

## Архитектура (SOLID, DRY, KISS)

### Конфигурация (DRY)
Конфигурация создается один раз в конструкторе класса как свойство экземпляра:

```javascript
constructor() {
    // ... другие свойства ...
    
    // Конфигурация стрима (создается один раз)
    this.STREAM_CONFIG = {
        PORT: 81,                      // Порт камера-сервера
        MAX_RECONNECT_ATTEMPTS: 10,    // Максимум попыток
        BASE_RECONNECT_DELAY: 2000,    // Базовая задержка (мс)
        MAX_RECONNECT_DELAY: 30000,    // Максимальная задержка (мс)
        ERROR_DEBOUNCE: 1000           // Защита от дребезга (мс)
    };
}
```

### Методы (Single Responsibility)

#### `setupCameraStream()`
Главный метод инициализации стрима. Делегирует работу специализированным методам.

#### `initStreamState()`
Инициализирует состояние стрима. Отвечает только за создание объекта состояния.

#### `setupStreamHandlers(streamImg)`
Устанавливает обработчики событий для элемента изображения.

#### `handleStreamConnect()`
Обрабатывает успешное подключение стрима. Сбрасывает счетчик попыток переподключения.

#### `resetReconnectState()` - DRY
Сбрасывает состояние переподключения (счетчик попыток и таймаут). Используется при успешном подключении для подготовки к будущим обрывам.

#### `handleStreamError()`
Обрабатывает ошибку стрима с защитой от множественных срабатываний.

#### `attemptStreamReconnect()`
Управляет логикой переподключения с учетом лимита попыток и экспоненциальной задержкой.

#### `reconnectStream()`
Выполняет переподключение к стриму с cache-busting параметром.

#### `getStreamUrl()` - DRY
Единственное место формирования URL стрима. Используется во всех местах где нужен URL.

#### `toggleStreamFallback(show)` - DRY
Единый метод для показа/скрытия fallback оверлея.

#### `updateFallbackMessage(message)`
Обновляет текстовое сообщение в fallback оверлее.

## Использованные принципы

### SOLID
- **Single Responsibility**: Каждый метод выполняет одну функцию
- **Open/Closed**: Легко расширяется через `STREAM_CONFIG`
- **Liskov Substitution**: Совместимо с базовым классом `BaseRobotUI`
- **Interface Segregation**: Маленькие, сфокусированные методы
- **Dependency Inversion**: Использует абстракцию `Logger`

### DRY (Don't Repeat Yourself)
- URL стрима формируется в одном месте
- Конфигурация вынесена в `STREAM_CONFIG`
- Управление fallback через единый метод `toggleStreamFallback()`

### KISS (Keep It Simple, Stupid)
- Простая и понятная логика переподключения
- Четкое разделение ответственности между методами
- Легко читается и понимается
- Легко тестировать каждый метод отдельно

## Пользовательский опыт

### При обрыве потока
1. Пользователь видит fallback оверлей с сообщением
2. Система автоматически пытается переподключиться
3. Счетчик попыток информирует о прогрессе
4. При успехе - плавное скрытие оверлея

### При исчерпании попыток
- Отображается сообщение: "Не удалось восстановить видео поток. Обновите страницу."
- Пользователь может вручную обновить страницу для повторной попытки

## Технические детали

### HTML структура
```html
<div id="streamFallback" class="stream-fallback hidden">
    <div class="fallback-content">
        <div class="fallback-icon">📹</div>
        <div class="fallback-text">Видео поток недоступен</div>
        <div id="streamFallbackMessage" class="fallback-message">...</div>
        <div class="fallback-spinner"></div>
    </div>
</div>
```

### CSS стилизация
- Полупрозрачный черный фон с blur эффектом
- Центрированный контент в рамке
- Цветовая схема в стиле интерфейса (#00ff88)
- Плавные переходы (transition)
- Анимированный спиннер

## Тестирование

### Сценарии для проверки
1. **Нормальная работа**: Стрим работает без перебоев
2. **Временный обрыв**: Стрим восстанавливается через несколько секунд
3. **Долгий обрыв**: Исчерпание всех 10 попыток
4. **Множественные обрывы**: Повторяющиеся проблемы с сетью

### Как протестировать
1. Запустить робот
2. Отключить камера-сервер или WiFi
3. Наблюдать автоматическое переподключение
4. Включить связь и убедиться в восстановлении

## Совместимость

Работает во всех типах роботов:
- ✅ МикроБокс Классик
- ✅ МикроБокс Лайнер  
- ✅ МикроБокс Брейн

Поддерживается на всех устройствах:
- ✅ Desktop (Chrome, Firefox, Safari, Edge)
- ✅ Mobile (iOS Safari, Android Chrome)
- ✅ VR (Oculus Browser)
