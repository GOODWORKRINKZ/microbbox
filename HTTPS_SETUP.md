# HTTPS Setup для WebXR/VR

## Зачем нужен HTTPS?

WebXR API требует **secure context** (защищенное соединение) для доступа к датчикам и контроллерам VR. Браузеры позволяют WebXR только через:
- `https://` (HTTPS)
- `localhost` (только для разработки)

Когда вы подключаетесь к ESP32 в режиме AP по IP адресу `192.168.4.1`, это не localhost, поэтому **необходим HTTPS**.

## Архитектура серверов

МикРоББокс использует 3 сервера на разных портах:

| Порт | Сервер | Назначение | Технология |
|------|--------|------------|------------|
| 80 | HTTP | Веб-интерфейс, API команды | AsyncWebServer |
| 81 | HTTP | Видеострим с камеры | ESP-IDF httpd |
| 443 | HTTPS | Веб-интерфейс для VR | esp32_https_server |

Все три сервера работают **одновременно**:
- Обычные пользователи используют HTTP (порт 80)
- VR пользователи используют HTTPS (порт 443)
- Видеострим доступен на порту 81 для обоих

## Установка

### 1. Включить FEATURE_HTTPS

Откройте `include/config.h` и раскомментируйте:

```cpp
#define FEATURE_HTTPS   // HTTPS сервер для WebXR/VR (требуется для Oculus Quest)
```

### 2. Установить OpenSSL

OpenSSL нужен для генерации самоподписанного сертификата при сборке.

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install openssl
```

**macOS:**
```bash
brew install openssl
```

**Windows:**
- Скачайте установщик: https://slproweb.com/products/Win32OpenSSL.html
- Установите "Win64 OpenSSL Light"
- Добавьте в PATH: `C:\Program Files\OpenSSL-Win64\bin`

### 3. Собрать проект

```bash
platformio run
```

Во время сборки скрипт `scripts/generate_cert.py`:
1. Проверит наличие OpenSSL
2. Сгенерирует RSA 2048 бит приватный ключ
3. Создаст самоподписанный сертификат на 10 лет
4. Встроит сертификат в `include/cert.h`
5. Включит его в прошивку

**Примечание:** Генерация сертификата занимает ~5-10 секунд при первой сборке.

### 4. Загрузить прошивку

```bash
platformio run --target upload
```

## Использование

### Подключение с обычного устройства

**HTTP (работает как раньше):**
```
http://192.168.4.1
```

### Подключение с VR шлема

**HTTPS (необходимо для WebXR):**
```
https://192.168.4.1
```

#### Первое подключение
1. Откройте Oculus Browser на Quest
2. Введите адрес: `https://192.168.4.1`
3. Браузер покажет: **"Ваше подключение не защищено"**
4. Нажмите **"Дополнительно"** → **"Продолжить"**
5. Браузер запомнит сертификат для этого устройства
6. Теперь можете использовать кнопку **"🥽 VR"**

### Видеострим

Видеострим работает на отдельном порту и доступен по HTTP:
```
http://192.168.4.1:81/stream
```

Веб-интерфейс автоматически использует правильный URL для стрима.

## Технические детали

### Сертификат

- **Тип**: Самоподписанный X.509
- **Алгоритм**: RSA 2048 бит
- **Действителен**: 10 лет с момента генерации
- **Subject**: CN=microbbox.local, O=MicroBox, C=RU
- **Хранение**: Встроен в прошивку как константные строки

### Производительность

- **Дополнительная RAM**: ~40-50 KB на HTTPS соединение
- **CPU**: Шифрование TLS добавляет ~5-10% нагрузки
- **Задержка**: +10-20ms на установку соединения
- **Максимум соединений**: 2-3 одновременных HTTPS соединения

### Безопасность

**Самоподписанный сертификат:**
- ✅ Шифрует трафик (защита от перехвата)
- ✅ Позволяет работать WebXR
- ❌ Браузер показывает предупреждение
- ❌ Не верифицирован Certificate Authority

Для **локальной сети** это приемлемо. Для **публичного доступа** рекомендуется использовать настоящий сертификат.

### Отключение HTTPS

Если HTTPS не нужен:

1. Закомментируйте в `include/config.h`:
   ```cpp
   //#define FEATURE_HTTPS
   ```

2. Пересоберите проект

Это освободит ~50KB RAM и упростит код.

## Решение проблем

### OpenSSL не найден

**Ошибка:**
```
ОШИБКА: OpenSSL не найден!
```

**Решение:**
- Установите OpenSSL (см. раздел "Установка")
- Убедитесь что `openssl` доступен в PATH
- Проверьте: `openssl version`

### Сертификат не генерируется

**Проверьте:**
```bash
# Убедитесь что FEATURE_HTTPS включен
grep FEATURE_HTTPS include/config.h

# Удалите старый сертификат
rm -f include/cert.h

# Пересоберите
platformio run
```

### Quest не принимает сертификат

**Причина:** Сертификат изменился после обновления прошивки.

**Решение:**
1. Откройте настройки Quest Browser
2. Очистите данные сайта для `192.168.4.1`
3. Подключитесь заново и примите новый сертификат

### HTTPS сервер не запускается

**Проверьте в Serial Monitor:**
```
HTTPS сервер успешно запущен на порту 443
```

Если видите ошибку:
- Проверьте доступную RAM (HTTPS требует ~50KB свободно)
- Попробуйте отключить другие функции (NEOPIXEL, BUZZER)
- Перезагрузите ESP32

### WebXR всё равно не работает

**Проверьте:**
1. Используете ли HTTPS: `https://192.168.4.1` (не `http://`)
2. Приняли ли сертификат в браузере
3. Открывается ли страница без ошибок
4. Видна ли кнопка "🥽 VR" в интерфейсе
5. Работает ли в Chrome на телефоне (для теста)

## Дополнительно

### Генерация сертификата вручную

Если хотите создать свой сертификат:

```bash
# Генерация приватного ключа
openssl genrsa -out server_key.pem 2048

# Создание сертификата на 10 лет
openssl req -new -x509 \
  -key server_key.pem \
  -out server_cert.pem \
  -days 3650 \
  -subj "/CN=microbbox.local/O=MicroBox/C=RU"

# Скопируйте содержимое в include/cert.h
```

### mDNS с HTTPS

HTTPS сервер регистрируется в mDNS:
```
_https._tcp.local.
```

Можно подключаться по имени (если mDNS работает):
```
https://a1b2c3.microbbox.local
```

### Использование реального сертификата

Для публичного домена можно использовать Let's Encrypt:

1. Получите доменное имя
2. Настройте DNS на ваш внешний IP
3. Получите сертификат через certbot
4. Конвертируйте в PEM формат
5. Замените содержимое в `include/cert.h`

**Ограничение:** ESP32 в режиме AP не имеет доступа к интернету, поэтому Let's Encrypt сертификат нельзя обновлять автоматически.

## Выводы

- HTTPS **необходим** для WebXR на Quest
- Работает параллельно с HTTP
- Автоматическая генерация сертификата
- Браузер покажет предупреждение - это нормально
- Не влияет на обычных пользователей HTTP
