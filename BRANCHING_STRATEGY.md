# Стратегия ветвления и версионирования МикРоББокс

## Версионирование (Semantic Versioning)

Проект следует принципам [Semantic Versioning 2.0.0](https://semver.org/):

```
MAJOR.MINOR.PATCH
```

- **MAJOR** (основная) - несовместимые изменения API
- **MINOR** (дополнительная) - новая функциональность с обратной совместимостью
- **PATCH** (патч) - исправления ошибок с обратной совместимостью

## Структура веток

### Основные ветки

#### `main`
- **Назначение:** Стабильная production-ready версия
- **Содержит:** Только протестированные релизы
- **Правила:** 
  - Прямые коммиты запрещены
  - Обновляется только через merge из `release/*` веток
  - Каждый merge = новый релиз с тегом

#### `develop`
- **Назначение:** Ветка разработки текущей версии
- **Содержит:** Интеграция всех новых фич для следующего релиза
- **Правила:**
  - Feature ветки мержатся сюда
  - Регулярное тестирование
  - Подготовка к следующему релизу

### Ветки поддержки версий

#### `support/0.0.x`
- **Назначение:** Поддержка старой версии 0.0.x
- **Содержит:** Исправления критических ошибок для 0.0.x
- **Правила:**
  - Создана от последнего релиза 0.0.x (например, v0.0.5)
  - Только критические исправления (security, critical bugs)
  - Новая функциональность НЕ добавляется
  - Релизы: 0.0.6, 0.0.7, etc.

#### `support/0.1.x`
- **Назначение:** Поддержка текущей стабильной версии 0.1.x
- **Содержит:** Исправления ошибок для 0.1.x
- **Правила:**
  - Создается после первого релиза 0.1.0
  - Исправления ошибок и патчи
  - Новая функциональность добавляется в `develop`
  - Релизы: 0.1.1, 0.1.2, etc.

### Рабочие ветки

#### `feature/*`
- **Назначение:** Разработка новой функциональности
- **Создается от:** `develop`
- **Мержится в:** `develop`
- **Именование:** `feature/robot-types`, `feature/ota-updates`
- **Правила:**
  - Одна ветка = одна фича
  - Удаляется после merge

#### `bugfix/*`
- **Назначение:** Исправление ошибок в develop
- **Создается от:** `develop`
- **Мержится в:** `develop`
- **Именование:** `bugfix/camera-init`, `bugfix/utf8-encoding`

#### `hotfix/*`
- **Назначение:** Срочные исправления критических ошибок в production
- **Создается от:** `main` или `support/*`
- **Мержится в:** `main` (или `support/*`) И `develop`
- **Именование:** `hotfix/v0.1.1-security-fix`
- **Правила:**
  - Только критические проблемы
  - Минимальные изменения
  - Быстрый релиз

#### `release/*`
- **Назначение:** Подготовка к релизу
- **Создается от:** `develop`
- **Мержится в:** `main` И `develop`
- **Именование:** `release/v0.1.0`, `release/v0.2.0`
- **Правила:**
  - Обновление версий
  - Финальное тестирование
  - Обновление CHANGELOG
  - Исправление мелких багов
  - НЕТ новой функциональности

## Workflow для версий

### Разработка новой minor версии (0.1.0 → 0.2.0)

```bash
# 1. Создание feature веток
git checkout develop
git checkout -b feature/new-sensors

# 2. Разработка и тестирование
# ... commits ...

# 3. Merge в develop
git checkout develop
git merge --no-ff feature/new-sensors
git branch -d feature/new-sensors

# 4. Подготовка релиза
git checkout -b release/v0.2.0
# Обновить версию в target_config.h
# Обновить CHANGELOG.md
# Финальное тестирование

# 5. Релиз
git checkout main
git merge --no-ff release/v0.2.0
git tag -a v0.2.0 -m "Release version 0.2.0"
git push origin main --tags

# 6. Обратно в develop
git checkout develop
git merge --no-ff release/v0.2.0
git branch -d release/v0.2.0
```

### Поддержка старой версии (0.0.x)

```bash
# 1. Создание ветки поддержки (один раз)
git checkout v0.0.5  # последний релиз 0.0.x
git checkout -b support/0.0.x
git push origin support/0.0.x

# 2. Критическое исправление
git checkout support/0.0.x
git checkout -b hotfix/v0.0.6-critical-bug
# ... исправления ...

# 3. Релиз патча
git checkout support/0.0.x
git merge --no-ff hotfix/v0.0.6-critical-bug
git tag -a v0.0.6 -m "Critical bugfix for 0.0.x"
git push origin support/0.0.x --tags

# 4. Применить в develop если актуально
git checkout develop
git cherry-pick <commit-hash>  # или git merge если возможно
```

### Исправление бага в текущей версии (0.1.x)

```bash
# 1. Создание hotfix
git checkout support/0.1.x
git checkout -b hotfix/v0.1.1-gpio-fix
# ... исправления ...

# 2. Релиз патча
git checkout support/0.1.x
git merge --no-ff hotfix/v0.1.1-gpio-fix
git tag -a v0.1.1 -m "GPIO4 button fix"
git push origin support/0.1.x --tags

# 3. Merge в main
git checkout main
git merge --no-ff support/0.1.x

# 4. Merge в develop
git checkout develop
git merge --no-ff support/0.1.x
```

## Политика поддержки версий

### Активная поддержка
- **Текущая MINOR версия** (0.1.x): полная поддержка
  - Исправление багов
  - Обновления безопасности
  - Патчи совместимости

### Ограниченная поддержка
- **Предыдущая MINOR версия** (0.0.x): 6 месяцев после релиза 0.1.0
  - Только критические баги
  - Только обновления безопасности
  - Нет новой функциональности

### Конец поддержки (EOL)
- После 6 месяцев версия 0.0.x переходит в статус EOL
- Рекомендуется обновление на 0.1.x

## GitHub Releases

### Именование релизов
- **Stable:** `v0.1.0`, `v0.1.1`, `v0.2.0`
- **Pre-release:** `v0.1.0-rc.1`, `v0.1.0-beta.1`
- **Development:** `v0.1.0-dev`

### Assets для релиза
Каждый релиз включает:
- `microbox-classic-v0.1.0-release.bin`
- `microbox-liner-v0.1.0-release.bin`
- `microbox-brain-v0.1.0-release.bin`
- `CHANGELOG.md` (или `CHANGELOG_0.1.md`)
- `README.md` с инструкциями

### Release Notes шаблон
```markdown
## МикРоББокс v0.1.0

### ✨ Новое
- Поддержка трех типов роботов (Classic, Liner, Brain)
- Автоматический выбор прошивки при OTA

### 🔧 Исправлено
- GPIO4 конфликт с камерой
- UTF-8 кодировка в веб-интерфейсе

### 🔄 Изменено
- Архитектура переработана по SOLID принципам

### ⚠️ Breaking Changes
- Требуется переконфигурация для типа робота
- Старые настройки не совместимы

### 📦 Бинарные файлы
Выберите файл согласно типу вашего робота.

### 🔗 Документация
См. [QUICKSTART.md](./QUICKSTART.md)
```

## Миграция между версиями

### 0.0.x → 0.1.x
Пользователи могут обновиться:
1. Через OTA (если доступно в 0.0.x)
2. Через USB (прямая прошивка)

**Важно:** После обновления требуется:
- Переконфигурировать тип робота
- Проверить настройки WiFi
- Загрузить файлы в SPIFFS

## Автоматизация

### GitHub Actions
Создать workflows для:
- **CI:** Автоматическая сборка всех targets на каждый push
- **Release:** Автоматическая сборка бинарников при создании тега
- **Support branches:** Автоматическое создание support веток

### Пример workflow для релиза
```yaml
name: Release Build

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [classic-release, liner-release, brain-release]
    steps:
      - uses: actions/checkout@v3
      - name: Set up PlatformIO
        run: pip install platformio
      - name: Build ${{ matrix.target }}
        run: pio run -e ${{ matrix.target }}
      - name: Upload to Release
        uses: actions/upload-release-asset@v1
        # ... загрузка .bin файла
```

## Текущее состояние (ноябрь 2025)

- **main:** Готовится к первому релизу 0.1.0
- **develop:** Активная разработка v0.1.0
- **support/0.0.x:** Будет создана после релиза 0.1.0

## Контрольный список для релиза

- [ ] Обновлен `GIT_VERSION` в `target_config.h`
- [ ] Обновлен `CHANGELOG_X.Y.md`
- [ ] Пройдены все тесты
- [ ] Проверена компиляция всех 6 targets
- [ ] Создан Git tag
- [ ] Собраны все бинарные файлы
- [ ] Создан GitHub Release с assets
- [ ] Обновлена документация
- [ ] Создана ветка `support/X.Y.x` (если нужно)

## Полезные команды

```bash
# Просмотр всех веток
git branch -a

# Просмотр всех тегов
git tag -l

# Создание новой ветки поддержки
git checkout -b support/0.1.x v0.1.0

# Сравнение веток
git diff support/0.0.x..support/0.1.x

# История релизов
git log --oneline --decorate --tags
```
