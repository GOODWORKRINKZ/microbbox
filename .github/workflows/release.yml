name: МикРоББокс Release

# Атомарный релиз с защитой от скачивания незавершенной сборки
# Порядок выполнения:
# 1. Вычисление версии
# 2. Сборка firmware (release и debug)
# 3. Подготовка файлов и контрольных сумм
# 4. Создание черновика релиза с тегом и всеми файлами
# 5. Публикация релиза (делает его видимым пользователям)
# 
# Преимущества:
# - Тег создается только с готовыми артефактами
# - Пользователи никогда не видят незавершенный релиз
# - Атомарная операция: либо все файлы доступны, либо ничего

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Тип релиза'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write

jobs:
  create-release:
    name: Create Release and Build Firmware
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Calculate Next Version
      id: version
      run: |
        chmod +x scripts/calculate_next_version.sh
        NEXT_VERSION=$(./scripts/calculate_next_version.sh ${{ inputs.release_type }})
        echo "version=${NEXT_VERSION}" >> $GITHUB_OUTPUT
        echo "Calculated next version: ${NEXT_VERSION}"
        
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Cache PlatformIO
      uses: actions/cache@v4
      with:
        path: ~/.platformio
        key: ${{ runner.os }}-pio-${{ hashFiles('**/platformio.ini') }}
        restore-keys: |
          ${{ runner.os }}-pio-
          
    - name: Install PlatformIO and dependencies
      run: |
        python -m pip install --upgrade pip
        pip install platformio htmlmin jsmin csscompressor
        
    - name: Get Previous Tag
      id: prev_tag
      run: |
        # Получаем предыдущий тег ДО создания нового
        PREV_TAG=$(git describe --tags --abbrev=0 --match='v*' 2>/dev/null || echo "")
        echo "prev_tag=${PREV_TAG}" >> $GITHUB_OUTPUT
        echo "Previous tag: ${PREV_TAG}"
        
    - name: Detect Branch Type
      id: branch_type
      run: |
        CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
        VERSION="${{ steps.version.outputs.version }}"
        
        # Проверяем, это support ветка или нет
        if [[ "$CURRENT_BRANCH" == support/* ]]; then
          echo "type=support" >> $GITHUB_OUTPUT
          echo "is_patch=true" >> $GITHUB_OUTPUT
          
          # Извлекаем base version (X.Y) из support/X.Y.x
          BASE_VERSION=$(echo "$CURRENT_BRANCH" | sed 's/support\/\([0-9]*\.[0-9]*\)\.x/\1/')
          echo "base_version=${BASE_VERSION}" >> $GITHUB_OUTPUT
          
          echo "🔧 Hotfix релиз из ${CURRENT_BRANCH}"
          echo "📦 Base версия: ${BASE_VERSION}"
        else
          echo "type=main" >> $GITHUB_OUTPUT
          echo "is_patch=false" >> $GITHUB_OUTPUT
          echo "🚀 Стабильный релиз из ${CURRENT_BRANCH}"
        fi
        
    - name: Generate Epic Release Name
      id: epic_name
      run: |
        VERSION="${{ steps.version.outputs.version }}"
        IS_PATCH="${{ steps.branch_type.outputs.is_patch }}"
        BASE_VERSION="${{ steps.branch_type.outputs.base_version }}"
        
        # Массивы старославянских прилагательных и героев
        ADJECTIVES=(
          "Yarostniy:Яростный"
          "Mudriy:Мудрый"
          "Hrabriy:Храбрый"
          "Veshchiy:Вещий"
          "Moguchiy:Могучий"
          "Slavniy:Славный"
          "Grozniy:Грозный"
          "Bystriy:Быстрый"
          "Svetliy:Светлый"
          "Krepkiy:Крепкий"
          "Silniy:Сильный"
          "Udaloy:Удалой"
          "Otvazhniy:Отважный"
          "Verniy:Верный"
          "Braviy:Бравый"
          "Velikiy:Великий"
          "Vechniy:Вечный"
          "Hitriy:Хитрый"
          "Shchedriy:Щедрый"
          "Neukrotimiy:Неукротимый"
        )
        
        HEROES=(
          "Svyatogor:Святогор"
          "Dobrynya:Добрыня"
          "Ilya:Илья"
          "Boyan:Боян"
          "Mikula:Микула"
          "Peresvet:Пересвет"
          "Volkh:Волх"
          "Stavr:Ставр"
          "Alyosha:Алёша"
          "Dunay:Дунай"
          "Volga:Вольга"
          "Churilo:Чурило"
          "Yeruslan:Еруслан"
          "Sadko:Садко"
          "Dyuk:Дюк"
          "Vladimir:Владимир"
          "Koshchey:Кощей"
          "Solovey:Соловей"
          "Potyk:Потык"
          "Buslay:Буслай"
        )
        
        # Эпичные древнеславянские суффиксы для hotfix релизов
        HOTFIX_SUFFIXES_EN=(
          "Skoraya Zastava"      # Скорая Застава - быстрая защита границ
          "Kriepkiy Shchit"      # Крепкий Щит - надежная оборона
          "Ognennoye Kopyo"      # Огненное Копьё - пронзает проблему
          "Miechenosiec"         # Меченосец - воин-исправитель
          "Velesova Mudrost"     # Велесова Мудрость - божественное решение
        )
        
        HOTFIX_SUFFIXES_RU=(
          "Скорая Застава"
          "Крепкий Щит"
          "Огненное Копьё"
          "Меченосец"
          "Велесова Мудрость"
        )
        
        if [ "$IS_PATCH" == "true" ]; then
          # Для hotfix используем base version для генерации имени
          SEED=$(echo -n "${BASE_VERSION}" | md5sum | cut -c1-8)
          SEED_DEC=$((16#$SEED))
          
          # Генерируем базовое эпическое имя от base version
          ADJ_INDEX=$(($SEED_DEC % ${#ADJECTIVES[@]}))
          HERO_INDEX=$((($SEED_DEC / ${#ADJECTIVES[@]}) % ${#HEROES[@]}))
          
          ADJ_PAIR="${ADJECTIVES[$ADJ_INDEX]}"
          HERO_PAIR="${HEROES[$HERO_INDEX]}"
          
          ADJ_EN=$(echo $ADJ_PAIR | cut -d: -f1)
          ADJ_RU=$(echo $ADJ_PAIR | cut -d: -f2)
          HERO_EN=$(echo $HERO_PAIR | cut -d: -f1)
          HERO_RU=$(echo $HERO_PAIR | cut -d: -f2)
          
          BASE_EPIC_EN="${ADJ_EN} ${HERO_EN}"
          BASE_EPIC_RU="${ADJ_RU} ${HERO_RU}"
          
          # Выбираем суффикс по патч-номеру
          PATCH_NUM=$(echo "$VERSION" | cut -d. -f3)
          SUFFIX_INDEX=$((($PATCH_NUM - 1) % ${#HOTFIX_SUFFIXES_EN[@]}))
          
          SUFFIX_EN="${HOTFIX_SUFFIXES_EN[$SUFFIX_INDEX]}"
          SUFFIX_RU="${HOTFIX_SUFFIXES_RU[$SUFFIX_INDEX]}"
          
          EPIC_NAME_EN="${BASE_EPIC_EN}: ${SUFFIX_EN}"
          EPIC_NAME_RU="${BASE_EPIC_RU}: ${SUFFIX_RU}"
          
          echo "🔧 Hotfix релиз: базируется на ${BASE_VERSION}"
        else
          # Для major/minor релизов - новое уникальное имя
          SEED=$(echo -n "$VERSION" | md5sum | cut -c1-8)
          SEED_DEC=$((16#$SEED))
          
          ADJ_INDEX=$(($SEED_DEC % ${#ADJECTIVES[@]}))
          HERO_INDEX=$((($SEED_DEC / ${#ADJECTIVES[@]}) % ${#HEROES[@]}))
          
          ADJ_PAIR="${ADJECTIVES[$ADJ_INDEX]}"
          HERO_PAIR="${HEROES[$HERO_INDEX]}"
          
          ADJ_EN=$(echo $ADJ_PAIR | cut -d: -f1)
          ADJ_RU=$(echo $ADJ_PAIR | cut -d: -f2)
          HERO_EN=$(echo $HERO_PAIR | cut -d: -f1)
          HERO_RU=$(echo $HERO_PAIR | cut -d: -f2)
          
          EPIC_NAME_EN="${ADJ_EN} ${HERO_EN}"
          EPIC_NAME_RU="${ADJ_RU} ${HERO_RU}"
          
          echo "🚀 Стабильный релиз: новое эпическое имя"
        fi
        
        echo "epic_name_en=${EPIC_NAME_EN}" >> $GITHUB_OUTPUT
        echo "epic_name_ru=${EPIC_NAME_RU}" >> $GITHUB_OUTPUT
        
        echo "🎭 Эпическое название релиза: ${EPIC_NAME_RU} (${EPIC_NAME_EN})"
        
    - name: Build All Robot Types - Release
      run: |
        echo "🔨 Building all robot types (release only)..."
        pio run --environment classic-release --environment liner-release --environment brain-release
        
    - name: Prepare Release Files
      run: |
        VERSION=${{ steps.version.outputs.version }}
        
        # Check for built firmware files (release only)
        echo "Checking for release firmware files..."
        
        # Array of robot types
        ROBOT_TYPES=("classic" "liner" "brain")
        
        # Check all release builds exist
        for robot in "${ROBOT_TYPES[@]}"; do
          BUILD_DIR=".pio/build/${robot}-release"
          if [ ! -f "${BUILD_DIR}/firmware.bin" ]; then
            echo "ERROR: ${BUILD_DIR}/firmware.bin not found!"
            exit 1
          fi
          echo "✅ Found: ${BUILD_DIR}/firmware.bin"
        done
        
        # Copy and rename firmware files for all robot types (release only)
        for robot in "${ROBOT_TYPES[@]}"; do
          BUILD_DIR=".pio/build/${robot}-release"
          OUTPUT_NAME="microbox-${robot}-${VERSION}.bin"
          
          cp "${BUILD_DIR}/firmware.bin" "${OUTPUT_NAME}"
          echo "📦 Created: ${OUTPUT_NAME}"
          
          # Создаем контрольную сумму
          sha256sum "${OUTPUT_NAME}" > "${OUTPUT_NAME}.sha256"
        done
        
        # Информация о размерах
        echo "## 📦 Собранные файлы:" > build-info.txt
        ls -lh microbox-*.bin | awk '{print $9 " - " $5}' >> build-info.txt
        
    - name: Generate Release Notes
      run: |
        VERSION=${{ steps.version.outputs.version }}
        EPIC_NAME_RU="${{ steps.epic_name.outputs.epic_name_ru }}"
        EPIC_NAME_EN="${{ steps.epic_name.outputs.epic_name_en }}"
        
        # Используем предыдущий тег, полученный до создания нового
        PREV_TAG="${{ steps.prev_tag.outputs.prev_tag }}"
        
        # Генерируем changelog
        CHANGELOG=""
        if [ -n "$PREV_TAG" ]; then
          echo "📝 Генерация changelog от $PREV_TAG до HEAD..."
          
          # Получаем количество коммитов
          COMMIT_COUNT=$(git rev-list "${PREV_TAG}..HEAD" --no-merges --count)
          
          if [ "$COMMIT_COUNT" -gt 0 ]; then
            # Получаем коммиты с улучшенным форматированием
            COMMITS=$(git log --pretty=format:"- %s" "${PREV_TAG}..HEAD" --no-merges)
            
            # Получаем список изменённых файлов для статистики
            FILES_CHANGED=$(git diff --name-only "${PREV_TAG}..HEAD" | wc -l)
            
            CHANGELOG=$(cat << CHANGELOG_EOF
        
        ## 🆕 Что нового в этой версии
        
        **Обновление с версии ${PREV_TAG}** (${COMMIT_COUNT} коммитов, изменено файлов: ${FILES_CHANGED})
        
        ### Изменения:
        
        ${COMMITS}
        
        CHANGELOG_EOF
        )
          else
            CHANGELOG=$(cat << CHANGELOG_EOF
        
        ## 🆕 Что нового в этой версии
        
        Технический релиз без изменений кода относительно ${PREV_TAG}.
        
        CHANGELOG_EOF
        )
          fi
        else
          echo "⚠️  Предыдущий тег не найден - это первый релиз"
          CHANGELOG=$(cat << CHANGELOG_EOF
        
        ## 🆕 Что нового в этой версии
        
        🎉 **Первый релиз МикроББокс!**
        
        Включает все базовые функции миниатюрного робота на ESP32CAM с веб-управлением.
        
        CHANGELOG_EOF
        )
        fi
        
        cat > RELEASE_NOTES.md << 'EOFMARKER'
        # МикРоББокс $VERSION — $EPIC_NAME_RU
        
        > **"$EPIC_NAME_EN"** — Epic codename for this release
        
        Автоматический релиз МикРоББокс - миниатюрного робота на ESP32CAM.
        
        $CHANGELOG_PLACEHOLDER
        
        ## 📦 Файлы прошивки
        
        В этом релизе доступны оптимизированные прошивки для **трех типов роботов**:
        
        ### 🚗 МикроБокс Классик (Classic)
        Полнофункциональный управляемый робот с веб-интерфейсом, видеостримом и эффектами.
        
        - **microbox-classic-$VERSION.bin** - Оптимизированная release версия
        
        ### 🛤️ МикроБокс Лайнер (Liner)
        Автономный робот следующий по линии с PID-контроллером.
        
        - **microbox-liner-$VERSION.bin** - Оптимизированная release версия
        
        ### 🎮 МикроБокс Брейн (Brain)
        Модуль управления для других роботов с трансляцией протоколов (PWM/PPM/SBUS/TBS).
        
        - **microbox-brain-$VERSION.bin** - Оптимизированная release версия
        
        > 💡 **Какой тип выбрать?** Для обычного управляемого робота используйте **Classic**. Для автономного следования по линии - **Liner**. Для подключения к другим роботам - **Brain**.
        > 
        > 🐛 **Нужна debug версия?** Debug версии с расширенным логированием доступны в исходном коде. Соберите их с помощью PlatformIO: `pio run -e classic-debug`
        
        ## 🚀 Как установить
        
        ### Метод 1: Через веб-интерфейс (OTA Update)
        
        1. Подключитесь к роботу через WiFi:
           - SSID: `МикРоББокс`
           - Пароль: `12345678`
        2. Откройте браузер и перейдите на:
           - `http://192.168.4.1` (в режиме точки доступа)
           - или `http://{6mac}.microbbox.local` (где {6mac} - последние 6 символов MAC, например: a1b2c3.microbbox.local)
        3. Нажмите кнопку **🔄 Обновление**
        4. Выберите нужный файл `.bin` для вашего типа робота (classic/liner/brain)
        5. Дождитесь завершения обновления
        
        > **Совет**: В веб-интерфейсе доступна настройка WiFi для подключения к вашей домашней сети!
        
        ### Метод 2: Через USB (esptool)
        
        ```bash
        # Установите esptool
        pip install esptool
        
        # Подключите ESP32CAM к компьютеру через USB-UART
        # Замените COM3 на ваш порт (для Linux/Mac: /dev/ttyUSB0)
        # Замените ROBOT_TYPE на нужный тип: classic, liner или brain
        esptool.py --chip esp32 --port COM3 --baud 921600 \
          write_flash -z 0x10000 microbox-ROBOT_TYPE-$VERSION.bin
        ```
        
        ### Метод 3: Через PlatformIO
        
        ```bash
        # Клонируйте репозиторий
        git clone https://github.com/GOODWORKRINKZ/microbbox.git
        cd microbbox
        
        # Переключитесь на нужную версию
        git checkout $VERSION
        
        # Соберите и загрузите нужный тип робота (release):
        # Classic (управляемый)
        pio run --environment classic-release --target upload
        
        # Liner (следование по линии)
        # pio run --environment liner-release --target upload
        
        # Brain (модуль управления)
        # pio run --environment brain-release --target upload
        ```
        
        ## ✨ Возможности
        
        - 📱 **Адаптивный веб-интерфейс** - автоматически подстраивается под ПК, мобильные устройства и VR-шлемы
        - 🎮 **Управление**:
          - С клавиатуры (WASD/стрелки)
          - Виртуальными стиками на мобильных
          - Геймпадом
          - Контроллерами VR (Oculus Quest)
        - 📹 **Видеострим в реальном времени** с камеры ESP32CAM
        - 💡 **3 адресных светодиода** WS2812B с анимациями
        - 🔊 **Бузер** для звуковых эффектов
        - 🚔 **Режимы эффектов**:
          - Полиция (красно-синие мигалки + сирена)
          - Пожарные (красно-оранжевые эффекты)
          - Скорая помощь (бело-красные эффекты)
          - Обычный (анимация движения)
        - 🔄 **OTA обновления** прямо через веб-интерфейс
        - ⚙️ **Два режима управления**:
          - Танковый (левый/правый стик = левая/правая сторона)
          - Дифференциальный (правый = скорость, левый = поворот)
        - 📡 **WiFi конфигурация**:
          - Настройка WiFi через веб-интерфейс
          - Автоматическое переключение между режимами Client и AP
          - mDNS поддержка (доступ по имени, например: a1b2c3.microbbox.local)
          - Хранение настроек в энергонезависимой памяти
        
        ## 🔧 Технические характеристики
        
        - **Процессор**: ESP32 @ 240MHz
        - **Память**: 320KB RAM, 4MB Flash
        - **Камера**: OV2640
        - **WiFi**: 2.4GHz (режимы AP/STA)
        - **Моторы**: 2x с драйвером MX1508
        - **Светодиоды**: 3x WS2812B (2 сзади, 1 спереди)
        - **Бузер**: Активный бузер на пине 16
        
        ## 📝 Проверка целостности
        
        Перед установкой проверьте контрольные суммы для выбранного типа робота:
        
        ```bash
        # МикроБокс Классик
        sha256sum -c microbox-classic-$VERSION.bin.sha256
        
        # МикроБокс Лайнер
        sha256sum -c microbox-liner-$VERSION.bin.sha256
        
        # МикроБокс Брейн
        sha256sum -c microbox-brain-$VERSION.bin.sha256
        ```
        
        ## 🐛 Известные проблемы
        
        - При первом запуске может потребоваться перезагрузка для стабильной работы WiFi
        - В режиме VR требуется совместимый браузер с WebXR API
        
        ## 📖 Документация
        
        Полная документация доступна в [README.md](https://github.com/GOODWORKRINKZ/microbbox/blob/main/README.md)
        
        ## 💬 Поддержка
        
        Если у вас возникли проблемы:
        - Создайте [Issue](https://github.com/GOODWORKRINKZ/microbbox/issues)
        - Посмотрите [Wiki](https://github.com/GOODWORKRINKZ/microbbox/wiki)
        
        ---
        
        EOFMARKER
        
        # Подставляем переменные в конец файла
        echo "" >> RELEASE_NOTES.md
        echo "**Дата сборки**: $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> RELEASE_NOTES.md
        echo "**Git commit**: $(git rev-parse --short HEAD)" >> RELEASE_NOTES.md
        
        # Заменяем переменные в файле на реальные значения
        sed -i "s/\$VERSION/${VERSION}/g" RELEASE_NOTES.md
        sed -i "s/\$EPIC_NAME_RU/${EPIC_NAME_RU}/g" RELEASE_NOTES.md
        sed -i "s/\$EPIC_NAME_EN/${EPIC_NAME_EN}/g" RELEASE_NOTES.md
        
        # Подставляем changelog (используем безопасный метод через переменную окружения)
        export CHANGELOG_CONTENT="$CHANGELOG"
        perl -i -pe 's/\$CHANGELOG_PLACEHOLDER/$ENV{CHANGELOG_CONTENT}/e' RELEASE_NOTES.md
        
        # Сохраняем changelog в файл для использования в summary
        echo "$CHANGELOG" > CHANGELOG_CONTENT.txt
        
    - name: Create Draft GitHub Release
      id: create_release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.version.outputs.version }}
        name: МикРоББокс ${{ steps.version.outputs.version }} — ${{ steps.epic_name.outputs.epic_name_ru }}
        body_path: RELEASE_NOTES.md
        files: |
          microbox-*.bin
          microbox-*.sha256
        draft: true  # Создаем как черновик - релиз НЕ виден пользователям
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.ADMIN_TOKEN }}
        
    - name: Publish Release
      run: |
        # Публикуем релиз атомарно: все файлы уже загружены, 
        # теперь делаем релиз видимым для пользователей
        gh release edit ${{ steps.version.outputs.version }} --draft=false
        
        echo "✅ Релиз ${{ steps.version.outputs.version }} успешно опубликован!"
      env:
        GH_TOKEN: ${{ secrets.ADMIN_TOKEN}}
        
    - name: Build Summary
      run: |
        echo "## ✅ Релиз ${{ steps.version.outputs.version }} успешно создан!" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🎭 Эпическое название:" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**${{ steps.epic_name.outputs.epic_name_ru }}** (${{ steps.epic_name.outputs.epic_name_en }})" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # Добавляем changelog в summary
        if [ -f CHANGELOG_CONTENT.txt ]; then
          cat CHANGELOG_CONTENT.txt >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "### 📦 Файлы:" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        ls -lh microbox-*.bin | awk '{print "- **" $9 "** - " $5}' >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🔗 Ссылки:" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "- [Релиз на GitHub](https://github.com/${{ github.repository }}/releases/tag/${{ steps.version.outputs.version }})" >> $GITHUB_STEP_SUMMARY
        echo "- [Все релизы](https://github.com/${{ github.repository }}/releases)" >> $GITHUB_STEP_SUMMARY
