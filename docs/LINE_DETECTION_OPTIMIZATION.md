# Оптимизация детектирования линии для ESP32-CAM

## Обзор

Документ описывает best practices и оптимизации алгоритма детектирования линии для робота МикроБокс Лайнер на ESP32-CAM.

## Применённые оптимизации

### 1. ✅ Использование Grayscale напрямую
**Статус**: Реализовано
```cpp
config.pixel_format = PIXFORMAT_GRAYSCALE;  // Не требует конвертации RGB
```
**Эффект**: Экономия ~40% CPU на конвертации цветов

### 2. ✅ Оптимизированное сканирование (4×4 линии)
**Статус**: Реализовано
```cpp
// Сканирование в 2 блока для кэш-локальности
// Блок 1: 4 горизонтальных линии подряд
// Блок 2: 4 вертикальных линии подряд
```
**Эффект**: 4x меньше промахов кэша, линейный доступ к памяти

### 3. ✅ Калибровка камеры
**Статус**: Реализовано
```cpp
#define LINE_PIXELS_PER_CM_WIDTH 16.0f
#define LINE_EXPECTED_WIDTH_PIXELS_H 31.2  // Для линии 19.5мм
```
**Эффект**: Фильтрация ложных срабатываний, взвешивание по уверенности

### 4. 🔄 ROI (Region of Interest) - НОВОЕ
**Статус**: В разработке
- Обработка только нижней части кадра (важнее для управления)
- Адаптивное сканирование на основе предыдущего кадра

### 5. 🔄 Адаптивная бинаризация - НОВОЕ
**Статус**: В разработке
- Динамический порог на основе метода Otsu
- Учёт изменения освещения

### 6. 🔄 Детектирование на отдельном ядре - НОВОЕ
**Статус**: В разработке
```cpp
// В РЕЖИМЕ СЛЕДОВАНИЯ ПО ЛИНИИ (веб/стрим ВЫКЛЮЧЕНЫ):
// Core 0: Детектирование линии (высокий приоритет, изолировано)
// Core 1: Управление моторами, PID, основной цикл
xTaskCreatePinnedToCore(lineDetectionTask, "LineDetect", 4096, this, 2, &taskHandle, 0);
```
**Эффект**: Стабильный FPS детектирования, параллельная обработка без конкуренции за ресурсы

**ВАЖНО**: В режиме следования WiFi/веб/стрим отключены - работают только камера, детектор, моторы!

### 7. 🔄 Отключение ресурсоёмких функций - НОВОЕ
**Статус**: В разработке
- Отключение веб-сервера в режиме следования
- Отключение видеострима
- Отключение NeoPixel LED
**Эффект**: Экономия 30-40% CPU и памяти

### 8. 🔄 Медианный фильтр позиции - НОВОЕ
**Статус**: В разработке
```cpp
float positions[5];  // Последние 5 значений
float median = getMedian(positions, 5);
```
**Эффект**: Сглаживание шума, стабильное управление

### 9. 🔄 Lookup таблица для нормализации - НОВОЕ
**Статус**: В разработке
```cpp
// Предвычисленная таблица pixel -> normalized position
static float normalizeLUT[LINE_CAMERA_WIDTH];
```
**Эффект**: Устранение деления в цикле

### 10. 🔄 Целочисленная арифметика - НОВОЕ
**Статус**: В разработке
- Использование int вместо float где возможно
- Битовые сдвиги вместо деления на степени 2
**Эффект**: Быстрее на 2-3x для простых операций

## Планируемые улучшения

### Приоритет 1: Критично для производительности
1. **Dual Core** - детектирование на Core 0
2. **Отключение веб/стрима** в режиме следования
3. **Адаптивная бинаризация** для разного освещения
4. **Медианный фильтр** для стабильности

### Приоритет 2: Важно для точности
1. **ROI оптимизация** - обработка только нижней части
2. **Адаптивное сканирование** на основе истории
3. **Калмановский фильтр** для предсказания

### Приоритет 3: Дополнительная оптимизация
1. **Lookup таблицы** для нормализации
2. **Целочисленная арифметика**
3. **PSRAM** для буферов
4. **Аппаратный таймер** для FPS

## Ожидаемые результаты

### Текущая производительность
- **FPS**: 20-25 (с веб-сервером)
- **Латентность**: ~40-50ms
- **CPU**: ~60-70% в режиме следования

### После оптимизации
- **FPS**: 40-50 (без веб-сервера)
- **Латентность**: ~20-25ms
- **CPU**: ~30-40% в режиме следования
- **Точность**: +15-20% за счёт фильтрации и калибровки

## Режим загрузки (Boot Mode)

### Configuration Mode (кнопка зажата при старте)
- ✅ Веб-сервер активен
- ✅ Видеострим активен
- ✅ WiFi активен
- ✅ Ручное управление доступно
- ✅ Обновление прошивки доступно

### Line Following Mode (кнопка не зажата при старте)
- ❌ Веб-сервер ПОЛНОСТЬЮ отключен
- ❌ Видеострим ПОЛНОСТЬЮ отключен
- ❌ WiFi ОТКЛЮЧЕН (максимальная экономия ресурсов)
- ✅ Работают ТОЛЬКО: камера, детектор линии, моторы
- ✅ Dual Core: Core 0 = детектирование, Core 1 = моторы/PID
- ✅ Максимальная производительность и стабильность
- ✅ Готов к старту следования сразу после включения

## Тестирование

### Метрики для измерения
1. **FPS детектирования** - кадров в секунду
2. **Латентность** - время от захвата до команды мотору
3. **CPU загрузка** - % использования каждого ядра
4. **Память** - использование heap/stack
5. **Точность** - отклонение от центра линии

### Инструменты
```cpp
// Измерение FPS
unsigned long frameTime = millis() - lastFrameTime;
float fps = 1000.0 / frameTime;

// Измерение CPU
esp_get_free_heap_size();
uxTaskGetStackHighWaterMark(NULL);
```

## Рекомендации по использованию

### Для соревнований (максимальная скорость)
- Использовать Line Following Mode
- LINE_BASE_SPEED = 70-80%
- Агрессивные PID коэффициенты

### Для отладки (стабильность)
- Использовать Configuration Mode
- LINE_BASE_SPEED = 40-50%
- Консервативные PID коэффициенты
- Включить DEBUG логи

### Для демонстрации
- Configuration Mode с видеострим
- Умеренная скорость 50-60%
- NeoPixel индикация (если включена)

## Дополнительные ресурсы

- `docs/CAMERA_CALIBRATION.md` - калибровка камеры
- `test/test_line_detection.py` - тестирование алгоритма
- `src/LinerRobot.cpp` - реализация детектирования
