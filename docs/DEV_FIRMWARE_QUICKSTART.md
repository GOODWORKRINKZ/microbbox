# Быстрый старт: Dev канал обновлений

## Для разработчиков

### Автоматическая сборка при push

```bash
# Любой push в develop или feature/* автоматически запускает сборку
git checkout develop
git add .
git commit -m "Добавлена новая функция"
git push origin develop

# GitHub Actions автоматически:
# 1. Соберет прошивку для всех типов (classic, liner, brain)
# 2. Создаст artifacts с dev версией
# 3. Сохранит на 30 дней
```

### Ручной запуск сборки

1. Перейти в [Actions → Dev Build](https://github.com/GOODWORKRINKZ/microbbox/actions/workflows/dev-build.yml)
2. Нажать **"Run workflow"**
3. Выбрать ветку
4. Включить "Создать draft release для OTA" (опционально)
5. Нажать **"Run workflow"**

## Для тестировщиков

### Переключение на dev канал

1. Подключиться к роботу (WiFi или USB)
2. Открыть веб-интерфейс
3. **⚙️ Настройки** → **🔄 Обновления**
4. В "Канал обновлений" выбрать **Dev ⚠️**
5. Нажать **"Проверить обновления"**

### Установка dev прошивки

**Вариант А: OTA (рекомендуется)**
1. После проверки обновлений увидите доступную dev версию
2. Нажать **"Обновить"**
3. Дождаться завершения

**Вариант Б: Скачать artifacts**
1. [Actions → Dev Build](https://github.com/GOODWORKRINKZ/microbbox/actions/workflows/dev-build.yml)
2. Выбрать нужный workflow run
3. Скачать artifacts (ZIP)
4. Распаковать и загрузить .bin через веб-интерфейс

### Возврат на stable

1. В настройках обновлений выбрать **Stable**
2. Проверить обновления
3. Установить последний stable релиз

## Формат dev версий

```
develop-a1b2c3d-20251106-185630
feature-new-sensors-e4f5g6h-20251106-190000
```

Где:
- `develop` / `feature-new-sensors` - имя ветки
- `a1b2c3d` - короткий SHA коммита
- `20251106-185630` - дата и время сборки UTC

## Отладка

### "Dev обновление не найдено"

Проверьте:
- ✅ Робот подключен к интернету
- ✅ Dev канал выбран в настройках
- ✅ На GitHub есть dev prerelease
- ✅ Prerelease опубликован (не draft)

### "Ошибка обновления"

1. Проверить WiFi соединение
2. Повторить попытку
3. Скачать .bin вручную и загрузить
4. Проверить Serial Monitor для деталей

### Откат

```bash
# Через USB-UART
esptool.py --chip esp32 --port /dev/ttyUSB0 \
  write_flash 0x10000 microbox-classic-v0.1.0-release.bin
```

## Полная документация

📖 [DEV_FIRMWARE_CHANNEL.md](DEV_FIRMWARE_CHANNEL.md) - подробная документация
