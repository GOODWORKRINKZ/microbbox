# Канал обновлений для разработчиков (Dev Channel)

## 🎯 Назначение

Система dev канала позволяет тестировать прошивки из веток `develop` и `feature/*` без создания официальных релизов. Это решает проблему тестирования новых функций до их выхода в стабильную версию.

## 📦 Два канала обновлений

### Stable (Стабильный)
- **По умолчанию**: Да
- **Источник**: Релизы из ветки `main`
- **Статус**: Production-ready, протестированные версии
- **Формат тега**: `v1.2.3`
- **Для кого**: Все пользователи

### Dev (Разработческий)
- **По умолчанию**: Нет
- **Источник**: Сборки из веток `develop` и `feature/*`
- **Статус**: Тестовые версии, могут содержать баги
- **Формат тега**: `dev-branch-name-commit-date`
- **Для кого**: Разработчики, тестировщики, ранние последователи

## 🔄 Автоматическая сборка dev прошивок

### Workflow Dev Build

При каждом push в `develop` или `feature/*` автоматически:

1. ✅ Собирается прошивка для всех типов роботов (classic, liner, brain)
2. ✅ Создаются артефакты с именами: `microbox-{type}-dev-{version}.bin`
3. ✅ Вычисляются контрольные суммы SHA256
4. ✅ Сохраняются как GitHub Actions artifacts (30 дней)
5. ✅ Создается draft prerelease для удобного OTA (опционально)

### Формат версии dev сборок

```
develop-a1b2c3d-20251106-185630
 ^       ^      ^
 |       |      └─ Дата и время сборки (UTC)
 |       └─ Короткий SHA коммита
 └─ Имя ветки (слэши заменены на дефисы)
```

Примеры:
- `develop-a1b2c3d-20251106-185630`
- `feature-new-sensors-e4f5g6h-20251106-190000`

## 🚀 Использование dev канала

### Способ 1: Через веб-интерфейс (рекомендуется)

1. Откройте веб-интерфейс робота
2. Перейдите в **⚙️ Настройки** → **🔄 Обновление прошивки**
3. В разделе настроек найдите **"Канал обновлений"**
4. Переключите с `stable` на `dev`
5. Нажмите **"Сохранить настройки"**
6. Нажмите **"Проверить обновления"**
7. Если доступна dev версия, появится кнопка **"Обновить"**

### Способ 2: Через API

```bash
# Установить dev канал
curl -X POST http://192.168.4.1/api/update/settings \
  -H "Content-Type: application/json" \
  -d '{"channel":"dev"}'

# Проверить обновления в dev канале
curl http://192.168.4.1/api/update/check?channel=dev

# Вернуться на stable канал
curl -X POST http://192.168.4.1/api/update/settings \
  -H "Content-Type: application/json" \
  -d '{"channel":"stable"}'
```

### Способ 3: Скачать артефакты вручную

Если нужна конкретная сборка:

1. Откройте [GitHub Actions](https://github.com/GOODWORKRINKZ/microbbox/actions/workflows/dev-build.yml)
2. Найдите нужный workflow run (по ветке и дате)
3. Скачайте артефакт из секции **Artifacts**
4. Распакуйте ZIP
5. Загрузите `.bin` файл через веб-интерфейс вручную

## 🔧 Как работает система

### При push в develop/feature

```yaml
push → GitHub Actions → Dev Build Workflow
  ↓
  1. Сборка firmware (classic, liner, brain)
  2. Создание артефактов с dev версией
  3. Сохранение как artifacts
  4. (Опционально) Создание draft prerelease
```

### При проверке обновлений в dev канале

```cpp
checkForUpdates(releaseInfo, FirmwareChannel::DEV)
  ↓
  1. Запрос к GitHub API: /repos/.../releases
  2. Поиск последнего prerelease с тегом "dev-*"
  3. Поиск файла: microbox-{robot_type}-dev-*.bin
  4. Возврат информации о доступном обновлении
```

### При установке обновления

```cpp
downloadAndInstallFirmware(url)
  ↓
  1. Загрузка .bin файла с GitHub
  2. Проверка целостности (ESP32 автоматически)
  3. OTA обновление
  4. Перезагрузка
```

## ⚠️ Важные замечания

### Безопасность

- ✅ Dev прошивки проходят те же проверки компиляции, что и stable
- ✅ ESP32 автоматически проверяет подпись прошивки перед установкой
- ⚠️ Код может быть не полностью протестирован
- ⚠️ Могут быть breaking changes без предупреждения

### Откат на stable

Если dev версия вызывает проблемы:

1. Переключитесь обратно на `stable` канал
2. Проверьте обновления - система найдет последний stable релиз
3. Обновитесь на stable версию

Или прошейте вручную последний stable релиз:
```bash
esptool.py --chip esp32 --port COM3 write_flash 0x10000 microbox-classic-v0.1.0-release.bin
```

### Retention (Срок хранения)

- **Artifacts**: 30 дней
- **Draft prereleases**: Бессрочно (пока не удалены вручную)

Рекомендуется:
- Для быстрого тестирования → Artifacts (скачать в течение 30 дней)
- Для длительного тестирования → Draft prereleases (создать вручную через workflow_dispatch)

## 🎬 Сценарии использования

### Сценарий 1: Разработчик тестирует новую фичу

```bash
# 1. Создал ветку feature/new-sensor
git checkout -b feature/new-sensor

# 2. Внес изменения и запушил
git add .
git commit -m "Добавлена поддержка нового сенсора"
git push origin feature/new-sensor

# 3. GitHub Actions собрал dev прошивку автоматически
# 4. Разработчик переключил робота на dev канал через веб-интерфейс
# 5. Проверил обновления → увидел feature-new-sensor-abc123d-...
# 6. Обновил через OTA
# 7. Протестировал новую фичу
```

### Сценарий 2: Тестировщик проверяет develop перед релизом

```bash
# 1. Перед релизом 0.2.0, тестировщик переключается на dev канал
# 2. В веб-интерфейсе выбирает "Канал: dev"
# 3. Проверяет обновления → видит develop-xyz789-...
# 4. Обновляется на последнюю dev версию
# 5. Тестирует все функции
# 6. Если OK → дает добро на релиз
# 7. Если баги → создает issues с номером dev версии
```

### Сценарий 3: Быстрое исправление в production

```bash
# 1. Обнаружен критический баг в stable v0.1.0
# 2. Разработчик создает hotfix ветку
git checkout -b hotfix/critical-bug main

# 3. Исправляет баг и пушит
git push origin hotfix/critical-bug

# 4. Dev build создает тестовую прошивку
# 5. Тестировщик проверяет fix на dev канале
# 6. Если OK → мержат в main и создают релиз v0.1.1
# 7. Пользователи обновляются на v0.1.1 через stable канал
```

## 📊 Мониторинг dev сборок

### Просмотр статуса сборок

GitHub Actions → [Dev Build Workflow](https://github.com/GOODWORKRINKZ/microbbox/actions/workflows/dev-build.yml)

### Просмотр доступных dev релизов

[Releases](https://github.com/GOODWORKRINKZ/microbbox/releases) → Фильтр "Pre-releases"

### Логирование на устройстве

```cpp
DEBUG_PRINTLN("Канал обновлений: dev");
DEBUG_PRINTLN("Проверка dev релизов...");
DEBUG_PRINTF("Найден dev релиз: %s\n", version);
```

## 🔍 Отладка

### Проблема: Dev обновление не находится

**Проверьте:**
1. ✅ Робот подключен к интернету
2. ✅ Dev канал активирован: `/api/update/settings` → `"channel":"dev"`
3. ✅ Существует prerelease с тегом `dev-*` на GitHub
4. ✅ В prerelease есть файл `microbox-{type}-dev-*.bin` для вашего типа робота
5. ✅ Prerelease не является draft (опубликован)

### Проблема: После обновления робот не загружается

**Решение:**
1. Подключите через USB-UART
2. Откройте Serial Monitor (115200 baud)
3. Посмотрите ошибки загрузки
4. Прошейте последнюю stable версию вручную:
   ```bash
   esptool.py --chip esp32 --port /dev/ttyUSB0 erase_flash
   esptool.py --chip esp32 --port /dev/ttyUSB0 write_flash 0x10000 microbox-classic-v0.1.0-release.bin
   ```

### Проблема: "Ошибка скачивания прошивки"

**Возможные причины:**
- Нестабильное WiFi соединение
- GitHub временно недоступен
- URL устарел (артефакт удален через 30 дней)

**Решение:**
- Проверьте WiFi соединение
- Повторите попытку позже
- Скачайте .bin файл вручную и загрузите через веб-интерфейс

## 🆕 Создание dev релиза вручную

Если нужно создать dev релиз для конкретной ветки:

1. Откройте [Actions](https://github.com/GOODWORKRINKZ/microbbox/actions/workflows/dev-build.yml)
2. Нажмите **"Run workflow"**
3. Выберите нужную ветку (develop или feature/*)
4. Включите опцию **"Создать draft release для OTA"**
5. Нажмите **"Run workflow"**
6. После сборки перейдите в Releases
7. Найдите draft release с тегом `dev-*`
8. Опубликуйте его (Edit → снять галочку "Draft" → "Publish")

## 📝 Best Practices

### Для разработчиков

- ✅ Пушьте в develop часто для автоматической сборки
- ✅ Тестируйте dev прошивки перед merge в main
- ✅ Указывайте в commit message ключевые изменения
- ✅ Создавайте draft releases для длительного тестирования

### Для тестировщиков

- ✅ Используйте отдельного робота для тестирования dev версий
- ✅ Документируйте баги с указанием dev версии
- ✅ Проверяйте develop перед каждым major/minor релизом
- ✅ Возвращайтесь на stable после тестирования

### Для пользователей

- ⚠️ Используйте stable канал для production
- ⚠️ Переходите на dev только если понимаете риски
- ✅ Делайте резервную копию настроек перед dev обновлением
- ✅ Сообщайте о багах в Issues с указанием dev версии

## 🔗 Связанные документы

- [BRANCHING_STRATEGY.md](../BRANCHING_STRATEGY.md) - Стратегия ветвления
- [FIRMWARE_UPDATE_SYSTEM.md](../FIRMWARE_UPDATE_SYSTEM.md) - Система обновлений
- [RELEASE_WORKFLOW.md](RELEASE_WORKFLOW.md) - Процесс создания релизов
- [CI workflow](../.github/workflows/ci.yml) - Основной CI
- [Dev Build workflow](../.github/workflows/dev-build.yml) - Dev сборки

---

**Версия документа:** 1.0  
**Дата создания:** 2025-11-06  
**Применимо к:** МикРоББокс v0.1.0+
