# Архитектура МикРоББокс 2.0

## 📋 Обзор

МикРоББокс 2.0 представляет собой модульную архитектуру, построенную на принципах SOLID, DRY и KISS. Проект поддерживает три типа роботов с единой кодовой базой:

1. **МикроБокс Классик** - Полнофункциональный управляемый робот
2. **МикроБокс Лайнер** - Автономный робот следующий по линии
3. **МикроБокс Брейн** - Модуль управления для других роботов

## 🏗️ Принципы архитектуры

### SOLID Принципы

#### Single Responsibility (Единственная ответственность)
Каждый класс имеет одну четко определенную ответственность:
- `MX1508MotorController` - только управление моторами
- `ClassicRobot` - только логика управляемого робота
- `LinerRobot` - только логика следования по линии
- `BrainRobot` - только трансляция протоколов

#### Open/Closed (Открыт для расширения, закрыт для изменения)
- Новые типы роботов добавляются через наследование от `BaseRobot`
- Новые контроллеры моторов реализуют интерфейс `IMotorController`
- Не требуется изменять существующий код

#### Liskov Substitution (Подстановка Барбары Лисков)
- Любой наследник `IRobot` может использоваться вместо базового класса
- Любая реализация `IMotorController` взаимозаменяема

#### Interface Segregation (Разделение интерфейсов)
- Маленькие, сфокусированные интерфейсы (`IComponent`, `IMotorController`)
- Компоненты реализуют только нужные им интерфейсы

#### Dependency Inversion (Инверсия зависимостей)
- Зависимость от абстракций (`IMotorController`), а не от конкретных реализаций
- `BaseRobot` работает с интерфейсом, не зная о `MX1508MotorController`

### DRY (Don't Repeat Yourself)
- Общая функциональность в `BaseRobot`
- Переиспользуемые компоненты (`MX1508MotorController`, `WiFiSettings`)
- Нет дублирования кода между типами роботов

### KISS (Keep It Simple, Stupid)
- Простая и понятная структура файлов
- Четкое разделение ответственности
- Минимум вложенности и сложности

## 📁 Структура проекта

```
microbbox/
├── include/
│   ├── target_config.h          # Конфигурация типов роботов
│   ├── hardware_config.h        # Конфигурация пинов и железа
│   ├── IComponent.h             # Базовый интерфейс компонента
│   ├── IRobot.h                 # Интерфейс робота
│   ├── IMotorController.h       # Интерфейс контроллера моторов
│   ├── BaseRobot.h              # Базовый класс робота
│   ├── ClassicRobot.h           # Классик робот
│   ├── LinerRobot.h             # Лайнер робот
│   ├── BrainRobot.h             # Брейн робот
│   ├── MX1508MotorController.h  # Контроллер моторов MX1508
│   ├── WiFiSettings.h           # Управление WiFi настройками
│   └── FirmwareUpdate.h         # Система OTA обновлений
├── src/
│   ├── main.cpp                 # Точка входа с фабрикой
│   ├── BaseRobot.cpp
│   ├── ClassicRobot.cpp
│   ├── LinerRobot.cpp
│   ├── BrainRobot.cpp
│   ├── MX1508MotorController.cpp
│   ├── WiFiSettings.cpp
│   └── FirmwareUpdate.cpp
└── platformio.ini               # Конфигурация сборки (ELRS стиль)
```

## 🎯 Типы роботов

### МикроБокс Классик (TARGET_CLASSIC)

**Назначение:** Полнофункциональный управляемый робот с камерой

**Компоненты:**
- ✅ Моторы (MX1508)
- ✅ Камера (OV2640)
- ✅ WiFi и веб-сервер
- ✅ NeoPixel LED (3 шт)
- ✅ Бузер
- ✅ OTA обновления
- ✅ Управление с телефона/компьютера/VR

**Управление:**
- Веб-интерфейс с видеостримом
- Виртуальные джойстики на мобильном
- Клавиатура на ПК (WASD)
- VR контроллеры (Oculus Quest)

**Эффекты:**
- Нормальный режим (индикация движения)
- Полиция (мигающие красно-синие)
- Пожарные (красно-оранжевые)
- Скорая помощь (бело-красные)
- Терминатор (красный HUD)

### МикроБокс Лайнер (TARGET_LINER)

**Назначение:** Автономный робот следующий по линии

**Компоненты:**
- ✅ Моторы (MX1508)
- ✅ Камера (OV2640) настроенная на 96x96 ЧБ
- ✅ WiFi и веб-сервер (для конфигурации)
- ✅ NeoPixel LED (индикация режима)
- ✅ Кнопка (запуск автономного режима)
- ✅ OTA обновления
- ✅ Опциональное ручное управление

**Режимы работы:**
1. **Ручной режим** (синяя индикация)
   - Управление через веб-интерфейс
   - Для позиционирования на старте линии
   
2. **Автономный режим** (зеленая индикация)
   - Активируется кнопкой
   - PID-контроллер для следования по линии
   - Камера анализирует изображение 96x96 ЧБ
   - Автоматическая корректировка траектории

**Алгоритм следования:**
- Захват кадра 96x96 grayscale
- Анализ нижней части изображения (75% высоты)
- Определение позиции линии (-1.0 слева, +1.0 справа)
- PID-регулятор (Kp=1.0, Ki=0.0, Kd=0.1)
- Дифференциальное управление моторами

**Настройки (hardware_config.h):**
```cpp
#define LINE_CAMERA_WIDTH 96
#define LINE_CAMERA_HEIGHT 96
#define LINE_THRESHOLD 128
#define LINE_PID_KP 1.0
#define LINE_PID_KI 0.0
#define LINE_PID_KD 0.1
#define LINE_BASE_SPEED 50
```

### МикроБокс Брейн (TARGET_BRAIN)

**Назначение:** Модуль управления для подключения к существующим роботам

**Компоненты:**
- ✅ Камера (OV2640) для видеостриминга
- ✅ WiFi и веб-сервер
- ✅ OTA обновления
- ✅ Трансляция протоколов управления
- ❌ Собственные моторы (подключается к внешнему роботу)

**Поддерживаемые протоколы вывода:**
1. **PWM** (1000-2000 мкс)
   - 4 канала на пинах 12-15
   - 50 Гц, 16-битное разрешение
   - Для стандартных servo/ESC
   
2. **PPM** (сумма)
   - 8 каналов
   - Выход на пин 12
   
3. **SBUS** (цифровой)
   - 16 каналов
   - 100k baud, инвертированный
   - UART на пин 12
   
4. **TBS Crossfire**
   - 8 каналов
   - 420k baud
   - CRSF протокол

**Примеры использования:**
- Добавление камеры и WiFi управления к роботу на платформе StackForce
- Управление дроном через веб-интерфейс
- Подключение к любому роботу с PWM/PPM/SBUS входами

## 🔧 Система конфигурации (ELRS стиль)

### Выбор типа робота через platformio.ini

Вместо директив препроцессора в коде, тип робота выбирается через build flags:

```ini
# МикроБокс Классик
[env:classic-debug]
build_flags = -D TARGET_CLASSIC -D DEBUG=1

[env:classic-release]
build_flags = -D TARGET_CLASSIC -D DEBUG=0 -O2

# МикроБокс Лайнер
[env:liner-debug]
build_flags = -D TARGET_LINER -D DEBUG=1

[env:liner-release]
build_flags = -D TARGET_LINER -D DEBUG=0 -O2

# МикроБокс Брейн
[env:brain-debug]
build_flags = -D TARGET_BRAIN -D DEBUG=1

[env:brain-release]
build_flags = -D TARGET_BRAIN -D DEBUG=0 -O2
```

### Автоматическая конфигурация компонентов

В `target_config.h` каждый тип робота автоматически включает нужные компоненты:

```cpp
#ifdef TARGET_CLASSIC
    #define FEATURE_MOTORS
    #define FEATURE_CAMERA
    #define FEATURE_WIFI
    #define FEATURE_WEBSERVER
    #define FEATURE_OTA_UPDATE
    #define FEATURE_NEOPIXEL
    #define FEATURE_BUZZER
    #define FEATURE_REMOTE_CONTROL
#endif

#ifdef TARGET_LINER
    #define FEATURE_MOTORS
    #define FEATURE_CAMERA
    #define FEATURE_WIFI
    #define FEATURE_WEBSERVER
    #define FEATURE_OTA_UPDATE
    #define FEATURE_NEOPIXEL
    #define FEATURE_BUTTON
    #define FEATURE_LINE_FOLLOWING
    #define FEATURE_REMOTE_CONTROL
#endif

#ifdef TARGET_BRAIN
    #define FEATURE_CAMERA
    #define FEATURE_WIFI
    #define FEATURE_WEBSERVER
    #define FEATURE_OTA_UPDATE
    #define FEATURE_PROTOCOL_TRANSLATOR
    #define FEATURE_PWM_OUTPUT
    #define FEATURE_PPM_OUTPUT
    #define FEATURE_SBUS_OUTPUT
    #define FEATURE_TBS_OUTPUT
    #define FEATURE_REMOTE_CONTROL
#endif
```

### Конфигурация пинов

В `hardware_config.h` определены пины для всех компонентов:

```cpp
// Моторы (для Classic и Liner)
#ifdef FEATURE_MOTORS
    #define MOTOR_LEFT_FWD_PIN 12
    #define MOTOR_LEFT_REV_PIN 13
    #define MOTOR_RIGHT_FWD_PIN 14
    #define MOTOR_RIGHT_REV_PIN 15
#endif

// LED (для Classic и Liner)
#ifdef FEATURE_NEOPIXEL
    #define NEOPIXEL_PIN 2
    #define NEOPIXEL_COUNT 3
#endif

// Кнопка (для Liner)
#ifdef FEATURE_BUTTON
    #define BUTTON_PIN 16
#endif

// Протоколы вывода (для Brain)
#ifdef FEATURE_PWM_OUTPUT
    #define PWM_OUT_PIN_1 12
    #define PWM_OUT_PIN_2 13
    #define PWM_OUT_PIN_3 14
    #define PWM_OUT_PIN_4 15
#endif
```

## 🛠️ Сборка проекта

### Сборка конкретного типа робота

```bash
# МикроБокс Классик
pio run --environment classic-debug
pio run --environment classic-release

# МикроБокс Лайнер  
pio run --environment liner-debug
pio run --environment liner-release

# МикроБокс Брейн
pio run --environment brain-debug
pio run --environment brain-release
```

### Загрузка в ESP32CAM

```bash
# Загрузка Classic робота
pio run --environment classic-release --target upload

# Загрузка Liner робота
pio run --environment liner-release --target upload

# Загрузка Brain робота
pio run --environment brain-release --target upload
```

### Проверка размера прошивки

```bash
pio run --environment classic-release --target size
pio run --environment liner-release --target size
pio run --environment brain-release --target size
```

## 🔌 Интерфейсы и абстракции

### IComponent

Базовый интерфейс для всех компонентов системы:

```cpp
class IComponent {
public:
    virtual bool init() = 0;          // Инициализация
    virtual void update() = 0;        // Обновление в loop
    virtual void shutdown() = 0;      // Завершение работы
    virtual bool isInitialized() const = 0;
};
```

### IRobot

Интерфейс для всех типов роботов:

```cpp
class IRobot : public IComponent {
public:
    virtual void loop() = 0;
    virtual IPAddress getIP() const = 0;
    virtual String getDeviceName() const = 0;
    virtual const char* getRobotType() const = 0;
};
```

### IMotorController

Интерфейс для контроллеров моторов:

```cpp
class IMotorController : public IComponent {
public:
    virtual void setSpeed(int leftSpeed, int rightSpeed) = 0;
    virtual void setMotorPWM(int throttlePWM, int steeringPWM) = 0;
    virtual void stop() = 0;
    virtual void getCurrentSpeed(int& leftSpeed, int& rightSpeed) const = 0;
};
```

## 🔄 Добавление нового типа робота

Для добавления нового типа робота (например, "Explorer"):

1. **Создать header файл** `include/ExplorerRobot.h`:
```cpp
#ifndef EXPLORER_ROBOT_H
#define EXPLORER_ROBOT_H

#include "BaseRobot.h"

#ifdef TARGET_EXPLORER

class ExplorerRobot : public BaseRobot {
public:
    ExplorerRobot();
    const char* getRobotType() const override { return "Explorer"; }
    
protected:
    bool initSpecificComponents() override;
    void updateSpecificComponents() override;
    void shutdownSpecificComponents() override;
    void setupWebHandlers(AsyncWebServer* server) override;
};

#endif
#endif
```

2. **Создать implementation файл** `src/ExplorerRobot.cpp`

3. **Добавить конфигурацию** в `target_config.h`:
```cpp
#ifdef TARGET_EXPLORER
    #define ROBOT_NAME "MicroBox-Explorer"
    #define FEATURE_MOTORS
    #define FEATURE_CAMERA
    // ... другие фичи
#endif
```

4. **Добавить targets** в `platformio.ini`:
```ini
[env:explorer-debug]
extends = env
build_flags =
    ${env.build_flags}
    -D TARGET_EXPLORER
    -D DEBUG=1

[env:explorer-release]
extends = env
build_flags =
    ${env.build_flags}
    -D TARGET_EXPLORER
    -D DEBUG=0 -O2
```

5. **Обновить main.cpp**:
```cpp
#ifdef TARGET_EXPLORER
#include "ExplorerRobot.h"
#endif

// В setup():
#ifdef TARGET_EXPLORER
    robot = new ExplorerRobot();
#endif
```

## 📊 Преимущества новой архитектуры

### ✅ Для разработки
- **Модульность**: Легко добавлять новые типы роботов
- **Переиспользование**: Общий код в `BaseRobot` и компонентах
- **Тестируемость**: Каждый компонент можно тестировать отдельно
- **Понятность**: Четкая структура и ответственность классов

### ✅ Для сборки
- **Чистая конфигурация**: Нет препроцессорных директив в коде
- **ELRS стиль**: Профессиональная система конфигурации
- **Множественные цели**: 6 готовых конфигураций сборки
- **Обратная совместимость**: Старые названия `debug`/`release` работают

### ✅ Для пользователей
- **Выбор типа**: Три разных робота из одной прошивки
- **Простота**: Выбрал тип при сборке - всё настроено автоматически
- **Расширяемость**: Легко добавлять свои типы роботов

## 🚀 Миграция со старой версии

Если у вас есть код на старой версии МикРоББокс:

1. **Classic робот** (обычный управляемый):
   - Собирайте `classic-release` вместо `release`
   - Или используйте `release` (обратная совместимость)

2. **Код с `MicroBoxRobot`**:
   - Функционал перенесен в `ClassicRobot`
   - `BaseRobot` содержит общую логику

3. **Конфигурация через `config.h`**:
   - Теперь автоматически через `target_config.h`
   - Не нужно вручную комментировать `#define`

## 📝 Дополнительные ресурсы

- [CONFIGURATIONS.md](CONFIGURATIONS.md) - Примеры конфигураций
- [README.md](README.md) - Основная документация
- [WIRING.md](WIRING.md) - Схемы подключения

---

**Версия:** 2.0  
**Дата:** 2025-11-02
