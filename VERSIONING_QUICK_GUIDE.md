# Краткое руководство по версионированию МикРоББокс

## 🎯 Быстрый старт

### Текущая версия: v0.1.0-dev
### Стратегия: Git Flow с поддержкой множественных версий

## 📊 Структура версий

```
0.0.x (legacy)  ─┐
                 ├─ Поддержка 6 месяцев
0.1.x (current) ─┤
                 ├─ Активная разработка
0.2.x (next)    ─┤
                 ├─ В планах
1.0.x (stable)  ─┘
```

## 🌳 Дерево веток

```
main (production)
├── support/0.0.x (maintenance, EOL через 6 мес)
├── support/0.1.x (stable, активная поддержка)
├── support/0.2.x (будет создана после релиза 0.2.0)
└── develop (разработка следующей версии)
    ├── feature/new-feature-1
    ├── feature/new-feature-2
    └── bugfix/some-bug
```

## 🔄 Жизненный цикл версии

### Фаза 1: Разработка (develop)
```bash
develop → feature/* → develop → release/v0.X.0
```

### Фаза 2: Релиз (main)
```bash
release/v0.X.0 → main (tag v0.X.0) → support/0.X.x
```

### Фаза 3: Поддержка (support/0.X.x)
```bash
support/0.X.x → hotfix/v0.X.Y → support/0.X.x (tag v0.X.Y)
```

### Фаза 4: EOL (End of Life)
```
Ветка архивируется, релизы прекращаются
```

## 📅 График поддержки версий

| Версия | Статус | Релиз | Конец поддержки | Примечания |
|--------|--------|-------|-----------------|------------|
| 0.0.x | EOL Planning | - | +6 мес после 0.1.0 | Критические баги только |
| 0.1.x | Active | Ноябрь 2025 | +6 мес после 0.2.0 | Полная поддержка |
| 0.2.x | Planned | Q1 2026 | +6 мес после 0.3.0 | В разработке |
| 1.0.x | Future | Q2 2026 | LTS (2 года) | Долгосрочная поддержка |

## 🚀 Сценарии использования

### 1️⃣ Новая фича для следующей версии

```bash
# Начало работы
git checkout develop
git pull origin develop
git checkout -b feature/awesome-sensors

# Разработка
# ... commits ...

# Завершение
git checkout develop
git merge --no-ff feature/awesome-sensors
git push origin develop
git branch -d feature/awesome-sensors
```

### 2️⃣ Исправление бага в текущей версии (0.1.x)

```bash
# Создание hotfix
git checkout support/0.1.x
git checkout -b hotfix/v0.1.5-gpio-fix

# Исправление
# ... commits ...

# Релиз патча
git checkout support/0.1.x
git merge --no-ff hotfix/v0.1.5-gpio-fix

# Обновление версии в коде
# Edit: include/target_config.h
#   #define GIT_VERSION "v0.1.5"
git commit -am "Bump version to 0.1.5"

# Создание тега
git tag -a v0.1.5 -m "Hotfix: GPIO4 button debounce"
git push origin support/0.1.x --tags

# Merge в main
git checkout main
git merge --no-ff support/0.1.x
git push origin main

# Merge в develop
git checkout develop
git merge --no-ff support/0.1.x
git push origin develop
```

### 3️⃣ Критический баг в старой версии (0.0.x)

```bash
# ТОЛЬКО критические баги!
git checkout support/0.0.x
git checkout -b hotfix/v0.0.7-security

# Минимальное исправление
# ... commits ...

# Релиз
git checkout support/0.0.x
git merge --no-ff hotfix/v0.0.7-security
git tag -a v0.0.7 -m "Security fix"
git push origin support/0.0.x --tags

# Cherry-pick в новые версии если актуально
git checkout support/0.1.x
git cherry-pick <commit-hash>
```

### 4️⃣ Подготовка нового релиза (0.2.0)

```bash
# 1. Создание release ветки
git checkout develop
git checkout -b release/v0.2.0

# 2. Финальная подготовка
# - Обновить target_config.h: GIT_VERSION "v0.2.0"
# - Создать CHANGELOG_0.2.md
# - Финальное тестирование
# - Исправление мелких багов (НЕТ новых фич!)

# 3. Merge в main
git checkout main
git merge --no-ff release/v0.2.0
git tag -a v0.2.0 -m "Release version 0.2.0"

# 4. Создание support ветки
git checkout -b support/0.2.x
git push origin support/0.2.x --tags

# 5. Обратно в develop
git checkout develop
git merge --no-ff release/v0.2.0
git push origin develop

# 6. Очистка
git branch -d release/v0.2.0

# 7. Обновить статус 0.1.x
# 0.1.x теперь в режиме ограниченной поддержки (6 месяцев)
```

### 5️⃣ Переход на мажорную версию (1.0.0)

```bash
# Для breaking changes
git checkout develop
git checkout -b release/v1.0.0

# Подготовка MAJOR релиза
# - BREAKING CHANGES должны быть задокументированы
# - Миграционный гайд для пользователей
# - Обновить все версии в коде

# Далее как обычный релиз
git checkout main
git merge --no-ff release/v1.0.0
git tag -a v1.0.0 -m "Major release 1.0.0"
git checkout -b support/1.0.x
git push origin support/1.0.x --tags

# 1.0.x получает LTS статус (2 года поддержки)
```

## 🏷️ Правила тегирования

### Формат тегов
```
v0.1.0      - Stable release
v0.1.0-rc.1 - Release candidate
v0.1.0-beta.1 - Beta version
v0.1.0-alpha.1 - Alpha version
v0.1.0-dev  - Development version
```

### Команды
```bash
# Создание тега
git tag -a v0.1.0 -m "Release 0.1.0: Multi-robot support"

# Просмотр тегов
git tag -l -n9  # с сообщениями

# Удаление тега (если ошибка)
git tag -d v0.1.0
git push origin :refs/tags/v0.1.0

# Push тегов
git push origin --tags
```

## 📦 Бинарные файлы для релизов

### Именование
```
microbox-{type}-v{version}-{build}.bin

Примеры:
microbox-classic-v0.1.0-release.bin
microbox-liner-v0.1.0-release.bin
microbox-brain-v0.1.0-release.bin

microbox-classic-v0.1.0-debug.bin  (для разработки)
```

### Сборка всех targets
```bash
# Release builds
pio run -e classic-release
pio run -e liner-release
pio run -e brain-release

# Debug builds
pio run -e classic-debug
pio run -e liner-debug
pio run -e brain-debug

# Копирование бинарников
cp .pio/build/*/firmware.bin releases/
```

## 🔐 Правила работы с ветками

### main
- ✅ Только через PR
- ✅ Обязательный code review
- ✅ Все тесты должны проходить
- ❌ Прямые commits запрещены

### develop
- ✅ Feature branches мержатся сюда
- ✅ Регулярное тестирование
- ⚠️ Может быть нестабильной

### support/*
- ✅ Только bugfix и hotfix
- ❌ Новая функциональность запрещена
- ⚠️ Минимальные изменения

### feature/*
- ✅ Создается от develop
- ✅ Одна ветка = одна фича
- ✅ Удаляется после merge

### hotfix/*
- ✅ Создается от main или support/*
- ✅ Только критические баги
- ✅ Быстрый релиз

## 📝 Контрольный список релиза

### Перед релизом
- [ ] Все фичи завершены и смержены в develop
- [ ] Пройдены все тесты
- [ ] Обновлены зависимости
- [ ] Проверена безопасность

### Подготовка релиза
- [ ] Создана ветка release/vX.Y.0
- [ ] Обновлен GIT_VERSION в target_config.h
- [ ] Создан CHANGELOG_X.Y.md
- [ ] Обновлена документация
- [ ] Финальное тестирование

### Релиз
- [ ] Merge в main
- [ ] Создан tag vX.Y.0
- [ ] Собраны все 3 бинарника (classic, liner, brain)
- [ ] Создан GitHub Release
- [ ] Загружены assets (бинарники + документация)
- [ ] Опубликованы Release Notes

### После релиза
- [ ] Создана ветка support/X.Y.x
- [ ] Merge обратно в develop
- [ ] Обновлен статус предыдущих версий
- [ ] Оповещены пользователи

## 🤖 Автоматизация (GitHub Actions)

### CI для всех веток
```yaml
# .github/workflows/ci.yml
on: [push, pull_request]
jobs:
  build:
    strategy:
      matrix:
        target: [classic-debug, liner-debug, brain-debug]
```

### Release при создании тега
```yaml
# .github/workflows/release.yml
on:
  push:
    tags:
      - 'v*.*.*'
jobs:
  build-and-release:
    # Сборка + создание GitHub Release
```

### Auto-create support branch
```yaml
# .github/workflows/support-branch.yml
on:
  push:
    tags:
      - 'v*.*.0'  # Только для X.Y.0
jobs:
  create-support-branch:
    # Создает support/X.Y.x автоматически
```

## 🔍 Отладка и диагностика

### Проверка текущей версии
```bash
# В коде
git describe --tags --always

# Какие ветки содержат коммит
git branch --contains <commit-hash>

# Разница между ветками
git log support/0.0.x..support/0.1.x --oneline
```

### Визуализация истории
```bash
# Красивое дерево
git log --graph --oneline --all --decorate

# Только теги
git log --tags --simplify-by-decoration --pretty="format:%d %s"
```

## 📚 Дополнительные ресурсы

- [BRANCHING_STRATEGY.md](./BRANCHING_STRATEGY.md) - Подробная стратегия ветвления
- [CHANGELOG_0.1.md](./CHANGELOG_0.1.md) - Список изменений
- [ARCHITECTURE.md](./ARCHITECTURE.md) - Архитектура проекта
- [Git Flow](https://nvie.com/posts/a-successful-git-branching-model/) - Основа нашей стратегии

## ❓ FAQ

**Q: Когда создавать support ветку?**
A: Сразу после релиза X.Y.0 (например, v0.1.0, v0.2.0)

**Q: Сколько версий поддерживать одновременно?**
A: 2-3 активных: текущая (полная) + предыдущая (ограниченная) + LTS если есть

**Q: Можно ли добавить фичу в support ветку?**
A: НЕТ! Только bugfix и security патчи.

**Q: Как мигрировать пользователей между версиями?**
A: OTA update выбирает правильный бинарник по типу робота автоматически.

**Q: Что делать с breaking changes?**
A: Только в MAJOR версиях (X.0.0). Задокументировать в CHANGELOG и создать миграционный гайд.

---

**Последнее обновление:** Ноябрь 2025
**Версия документа:** 1.0
