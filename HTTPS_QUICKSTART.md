# HTTPS для WebXR/VR - Краткое руководство

## Что изменилось?

Добавлена поддержка HTTPS для работы WebXR/VR на Oculus Quest!

## Зачем это нужно?

WebXR (виртуальная реальность в браузере) требует защищенное соединение HTTPS. 
При подключении Quest к ESP32 через WiFi AP режим по IP адресу `192.168.4.1`, 
браузер требует HTTPS для доступа к VR контроллерам.

## Быстрый старт

### 1. Включить HTTPS

Откройте `include/config.h` и убедитесь что включено:
```cpp
#define FEATURE_HTTPS   // HTTPS сервер для WebXR/VR
```

### 2. Установить OpenSSL

**Ubuntu/Debian:**
```bash
sudo apt-get install openssl
```

**macOS:**
```bash
brew install openssl
```

**Windows:**
Скачать: https://slproweb.com/products/Win32OpenSSL.html

### 3. Собрать и загрузить

```bash
platformio run --target upload
```

При первой сборке автоматически сгенерируется самоподписанный сертификат.

### 4. Подключиться с Quest

1. Подключитесь к WiFi `MICROBBOX-XXXXXX`
2. Откройте Quest Browser
3. Перейдите на: **`https://192.168.4.1`**
4. Примите предупреждение о сертификате
5. Нажмите кнопку **"🥽 VR"**

## Что работает?

- ✅ HTTP на порту 80 (как раньше)
- ✅ HTTPS на порту 443 (новое!)
- ✅ Видеострим на порту 81 (как раньше)
- ✅ WebXR/VR режим через HTTPS
- ✅ Обратная совместимость

## Порты

| Порт | Протокол | Что там |
|------|----------|---------|
| 80 | HTTP | Веб-интерфейс (обычный доступ) |
| 443 | HTTPS | Веб-интерфейс (для VR) |
| 81 | HTTP | Видеострим с камеры |

## Память

HTTPS добавляет ~40-50KB RAM overhead. 
Если нужно освободить память - закомментируйте `#define FEATURE_HTTPS` в config.h

## Проблемы?

### "OpenSSL не найден"
Установите OpenSSL (см. раздел "Установить OpenSSL")

### "Браузер показывает предупреждение"
Это нормально для самоподписанного сертификата! 
Нажмите "Дополнительно" → "Продолжить"

### "WebXR не работает"
1. Убедитесь что используете `https://` (не `http://`)
2. Проверьте что приняли сертификат в браузере
3. Проверьте что кнопка "🥽 VR" видна в интерфейсе

## Подробная документация

Смотрите [HTTPS_SETUP.md](HTTPS_SETUP.md) для детальной информации:
- Как это работает
- Технические детали
- Решение проблем
- Настройка сертификатов

## Вопросы?

Создайте Issue на GitHub с описанием проблемы.
