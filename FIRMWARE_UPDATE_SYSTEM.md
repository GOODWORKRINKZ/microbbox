# Система обновлений МикРоББокс 0.1

## 🎯 Множественные бинарные файлы при релизе

С версии 0.1.0 МикРоББокс поддерживает три типа роботов, и при каждом релизе создается **три отдельных бинарных файла**:

### Бинарные файлы релиза

```
microbox-classic-v0.1.0-release.bin   - для Classic робота
microbox-liner-v0.1.0-release.bin     - для Liner робота  
microbox-brain-v0.1.0-release.bin     - для Brain робота
```

### Именование файлов

Формат: `microbox-{type}-{version}-release.bin`

Где:
- `{type}` = `classic` | `liner` | `brain`
- `{version}` = версия релиза (например, `v0.1.0`)

## 🤖 Автоматический выбор прошивки

### Как это работает

1. **Робот знает свой тип**
   - Classic робот: `getRobotType()` возвращает `"Classic"`
   - Liner робот: `getRobotType()` возвращает `"Liner"`
   - Brain робот: `getRobotType()` возвращает `"Brain"`

2. **При проверке обновлений**
   - `FirmwareUpdate` получает тип робота при инициализации
   - Конвертирует в lowercase: `"Classic"` → `"classic"`
   - Ищет на GitHub релизе файл с соответствующим типом

3. **Поиск файла**
   ```cpp
   // Приоритет 1: Специфичный файл для типа робота
   String targetFilename = "microbox-" + robotType + "-" + version + "-release.bin";
   // Пример: microbox-classic-v0.1.0-release.bin
   
   // Приоритет 2: Любой файл содержащий тип робота в имени
   if (url.indexOf(robotType) >= 0 && url.endsWith("-release.bin"))
   
   // Приоритет 3: Универсальный файл (для обратной совместимости)
   if (url.endsWith("-release.bin"))
   ```

4. **Загрузка и установка**
   - Робот загружает только подходящий для него файл
   - Не может случайно установить прошивку другого типа

## 📦 Создание релиза

### GitHub Actions (автоматизация)

Рекомендуется создать GitHub Actions workflow для автоматической сборки всех типов:

```yaml
# .github/workflows/release.yml
name: Build Release

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        type: [classic, liner, brain]
        
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Install PlatformIO
        run: pip install platformio
        
      - name: Build ${{ matrix.type }}
        run: pio run --environment ${{ matrix.type }}-release
        
      - name: Rename binary
        run: |
          VERSION=${{ github.event.release.tag_name }}
          mv .pio/build/${{ matrix.type }}-release/firmware.bin \
             microbox-${{ matrix.type }}-$VERSION-release.bin
             
      - name: Upload to Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./microbox-${{ matrix.type }}-${{ github.event.release.tag_name }}-release.bin
          asset_name: microbox-${{ matrix.type }}-${{ github.event.release.tag_name }}-release.bin
          asset_content_type: application/octet-stream
```

### Ручная сборка

Если собираете вручную:

```bash
# 1. Соберите все три типа
pio run --environment classic-release
pio run --environment liner-release  
pio run --environment brain-release

# 2. Переименуйте бинарники
mv .pio/build/classic-release/firmware.bin microbox-classic-v0.1.0-release.bin
mv .pio/build/liner-release/firmware.bin microbox-liner-v0.1.0-release.bin
mv .pio/build/brain-release/firmware.bin microbox-brain-v0.1.0-release.bin

# 3. Создайте релиз на GitHub
# 4. Загрузите все три файла как assets
```

## 🔄 Процесс обновления

### Со стороны пользователя

1. Робот подключен к интернету
2. Открываем веб-интерфейс → Настройки → Обновления
3. Нажимаем "Проверить обновления"
4. Система автоматически:
   - Находит последний релиз на GitHub
   - Ищет файл для текущего типа робота
   - Показывает информацию о доступной версии
5. Нажимаем "Скачать обновление"
6. Робот скачивает и устанавливает правильную прошивку
7. Автоматическая перезагрузка

### Технический процесс

```cpp
// 1. Инициализация (в BaseRobot::init())
firmwareUpdate_ = new FirmwareUpdate();
firmwareUpdate_->setRobotType(String(getRobotType()).toLowerCase());

// 2. Проверка обновлений (при запросе пользователя)
ReleaseInfo releaseInfo;
bool hasUpdate = firmwareUpdate_->checkForUpdates(releaseInfo);

// 3. В checkForUpdates():
// - Запрос к GitHub API: /repos/{owner}/{repo}/releases/latest
// - Парсинг JSON ответа
// - Поиск файла: "microbox-" + robotType + "-" + version + "-release.bin"
// - Возврат информации о релизе

// 4. Загрузка (при подтверждении пользователя)
firmwareUpdate_->downloadAndInstallFirmware(releaseInfo.downloadUrl);
```

## 🛡️ Безопасность

### Защита от неправильной прошивки

1. **Проверка имени файла**
   - Файл должен содержать тип робота в имени
   - Формат имени строго проверяется

2. **Обратная совместимость**
   - Если специфичный файл не найден, используется универсальный
   - Позволяет обновляться с старых версий (0.0.X)

3. **Логирование**
   ```cpp
   DEBUG_PRINTF("Найден бинарник для %s: %s\n", robotType.c_str(), url.c_str());
   DEBUG_PRINTLN("ПРЕДУПРЕЖДЕНИЕ: Не найден подходящий бинарник!");
   ```

4. **Валидация перед установкой**
   - ESP32 проверяет подпись прошивки
   - Неправильная прошивка не будет установлена

## 📊 Пример релиза

### Структура релиза v0.1.0

```
Release: v0.1.0 - МикРоББокс с SOLID архитектурой
Assets:
├── microbox-classic-v0.1.0-release.bin   (1.2 MB)
├── microbox-liner-v0.1.0-release.bin     (1.1 MB)
├── microbox-brain-v0.1.0-release.bin     (1.0 MB)
└── CHANGELOG_0.1.md
```

### Что загрузит каждый тип:

- **Classic робот** → `microbox-classic-v0.1.0-release.bin`
- **Liner робот** → `microbox-liner-v0.1.0-release.bin`
- **Brain робот** → `microbox-brain-v0.1.0-release.bin`

## 🚀 Миграция со старых версий

### С версии 0.0.X

Старые версии (до 0.1.0) имели один универсальный бинарник:
- `firmware-v0.0.5-release.bin`

При обновлении с 0.0.X на 0.1.0:
1. Робот еще не знает о множественных типах
2. Скачивает универсальный файл (если есть)
3. После обновления на 0.1.0 начинает использовать специфичные файлы

### Поддержка универсального файла

Для плавной миграции система поддерживает поиск универсального файла:

```cpp
// Если не нашли специфичный файл
if (releaseInfo.downloadUrl.length() == 0) {
    // Ищем любой -release.bin файл
    if (url.endsWith("-release.bin")) {
        releaseInfo.downloadUrl = url;
    }
}
```

## 💡 Рекомендации

### Для разработчиков

1. **Всегда создавайте все три файла**
   - Даже если изменения касаются только одного типа
   - Пользователи ожидают обновления для своего типа

2. **Используйте GitHub Actions**
   - Автоматизирует процесс сборки
   - Исключает человеческие ошибки
   - Гарантирует правильные имена файлов

3. **Тестируйте каждый тип**
   - Проверяйте обновление для каждого типа
   - Убедитесь что файлы загружаются корректно

### Для пользователей

1. **Проверяйте тип робота**
   - В веб-интерфейсе отображается тип
   - API endpoint: `/api/robot-type`

2. **Стабильное соединение**
   - Обновление через WiFi требует стабильного соединения
   - Не отключайте питание во время обновления

3. **Резервное копирование**
   - Сохраните настройки перед обновлением
   - WiFi настройки сохраняются автоматически

## 🔍 Отладка

### Проверка типа робота

```cpp
// В Serial Monitor (115200 baud):
DEBUG_PRINTF("Тип робота: %s\n", getRobotType());
DEBUG_PRINTF("Тип для обновления: %s\n", firmwareUpdate->getRobotType().c_str());
```

### Проверка URL файла

```cpp
ReleaseInfo releaseInfo;
if (firmwareUpdate->checkForUpdates(releaseInfo)) {
    DEBUG_PRINTF("Download URL: %s\n", releaseInfo.downloadUrl.c_str());
}
```

### Если обновление не работает

1. Проверьте наличие файлов в релизе на GitHub
2. Проверьте формат имен файлов
3. Проверьте логи в Serial Monitor
4. Убедитесь что WiFi подключен

---

**Версия документа:** 1.0  
**Дата:** 2025-11-03  
**Применимо к:** МикРоББокс v0.1.0+
