# Анализ различий между кадрами пустого поля

## 🎯 Назначение

Этот скрипт предназначен для оценки различий между кадрами пустого поля, снятыми в разных местах. Используется для:

- **Калибровки камеры** - оценка стабильности работы камеры
- **Анализа шума матрицы** - измерение уровня шума сенсора
- **Проверки освещения** - оценка однородности освещения
- **Контроля качества** - проверка постоянства условий съемки

## 📋 Установка зависимостей

```bash
cd test
pip install -r requirements.txt
```

Требуемые пакеты:
- numpy
- pillow
- matplotlib
- opencv-python

## 🚀 Использование

### 1. Сравнение двух кадров

Базовое сравнение двух изображений:

```bash
python3 evaluate_frame_differences.py image1.jpg image2.jpg
```

**Пример:**
```bash
python3 evaluate_frame_differences.py \
    ../data/img_calibration/calibration_20251109_155647_232_005.jpg \
    ../data/img_calibration/calibration_20251109_155648_370_006.jpg
```

### 2. Анализ всей директории

Анализ всех изображений в папке:

```bash
python3 evaluate_frame_differences.py ../data/img_calibration/*.jpg
```

### 3. Анализ выборки кадров

Сравнение нескольких конкретных кадров:

```bash
python3 evaluate_frame_differences.py frame1.jpg frame2.jpg frame3.jpg frame4.jpg
```

## 📊 Метрики анализа

Скрипт вычисляет следующие метрики различия:

### 1. MSE (Mean Squared Error) - Среднеквадратичная ошибка
- **Что показывает:** Общую величину различий между кадрами
- **Интерпретация:**
  - < 100: минимальные различия
  - 100-500: умеренные различия
  - \> 500: значительные различия

### 2. MAE (Mean Absolute Error) - Средняя абсолютная ошибка
- **Что показывает:** Среднюю разницу яркости пикселей
- **Интерпретация:**
  - < 5: отличная стабильность
  - 5-15: хорошая стабильность
  - 15-30: умеренные колебания
  - \> 30: значительные колебания

### 3. SSIM (Structural Similarity Index) - Индекс структурного сходства
- **Что показывает:** Структурное сходство изображений
- **Диапазон:** от 0 (совершенно разные) до 1 (идентичные)
- **Интерпретация:**
  - \> 0.95: очень высокое сходство - отличная стабильность
  - 0.85-0.95: высокое сходство - хорошая стабильность
  - 0.70-0.85: умеренное сходство - заметные различия
  - < 0.70: низкое сходство - значительные различия

### 4. Корреляция гистограмм
- **Что показывает:** Сходство распределения яркости
- **Диапазон:** от -1 до 1
- **Интерпретация:**
  - \> 0.95: идентичное распределение
  - 0.85-0.95: очень похожее распределение
  - < 0.85: различающееся распределение

### 5. Статистика изображений
Для каждого кадра анализируется:
- Средняя яркость (mean)
- Стандартное отклонение (std) - характеризует шум
- Минимум и максимум яркости
- Медиана
- Перцентили (25%, 75%)

## 📈 Выходные данные

### Консольный вывод

1. **Статистика каждого изображения:**
   - Средняя яркость
   - Стандартное отклонение
   - Диапазон значений
   - Медиана

2. **Метрики различия:**
   - MSE, MAE, SSIM
   - Корреляция гистограмм
   - Процент значительно отличающихся пикселей

3. **Интерпретация:**
   - Оценка стабильности камеры
   - Оценка качества освещения
   - Рекомендации

4. **Общая статистика (для нескольких кадров):**
   - Разброс яркости между кадрами
   - Средний уровень шума
   - Среднее структурное сходство

### Визуализация

Создается файл `output/frame_differences_analysis.png` с:

1. **Все изображения** - визуальное сравнение
2. **График яркости** - изменение средней яркости по кадрам
3. **Тепловая карта SSIM** - матрица попарных сравнений
4. **Гистограммы** - распределение яркости всех кадров

## 🎓 Интерпретация результатов

### Сценарий 1: Отличная стабильность ✅

```
SSIM: > 0.90
MAE: < 10
Разброс яркости (σ): < 3

→ Камера стабильна
→ Освещение однородное
→ Можно использовать для калибровки
```

### Сценарий 2: Хорошая стабильность ✅

```
SSIM: 0.75-0.90
MAE: 10-20
Разброс яркости (σ): 3-8

→ Приемлемая стабильность
→ Небольшие колебания в пределах нормы
→ Можно использовать с осторожностью
```

### Сценарий 3: Умеренные проблемы ⚠️

```
SSIM: 0.60-0.75
MAE: 20-35
Разброс яркости (σ): 8-15

→ Заметные различия между кадрами
→ Рекомендуется:
  - Проверить крепление камеры
  - Проверить освещение
  - Убедиться в однородности поверхности
```

### Сценарий 4: Значительные проблемы ❌

```
SSIM: < 0.60
MAE: > 35
Разброс яркости (σ): > 15

→ Нестабильные условия
→ Требуется:
  - Переснять кадры
  - Проверить настройки камеры
  - Стабилизировать освещение
```

## 🔬 Что может влиять на различия

### Технические факторы:
- **Шум матрицы** - естественный шум сенсора камеры
- **Автоэкспозиция** - изменение параметров экспозиции между кадрами
- **Тепловой шум** - изменения при нагреве камеры
- **Компрессия JPEG** - артефакты сжатия

### Внешние факторы:
- **Освещение** - изменения в освещенности сцены
- **Позиция камеры** - небольшие сдвиги при съемке
- **Поверхность** - неоднородность "пустого" поля
- **Вибрации** - движение камеры/робота

## 💡 Рекомендации по съемке

Для получения качественных калибровочных кадров:

1. **Стабильное крепление**
   - Надежно зафиксировать камеру/робота
   - Избегать вибраций

2. **Постоянное освещение**
   - Снимать при одинаковом освещении
   - Избегать теней и бликов
   - Использовать рассеянный свет

3. **Однородная поверхность**
   - Использовать действительно пустую/однородную поверхность
   - Белый лист бумаги, матовая поверхность
   - Избегать текстур и узоров

4. **Настройки камеры**
   - Фиксированная экспозиция (отключить автоэкспозицию если возможно)
   - Фиксированный баланс белого
   - Постоянное разрешение

5. **Количество кадров**
   - Рекомендуется 10-20 кадров для надежной статистики
   - Снимать с небольшими интервалами времени

## 📁 Структура выходных файлов

```
test/
├── evaluate_frame_differences.py    # Основной скрипт
├── README_FRAME_DIFFERENCES.md      # Эта документация
└── output/
    └── frame_differences_analysis.png   # Визуализация результатов
```

## 🔧 Дополнительные возможности

### Пакетная обработка

Для автоматического анализа нескольких серий:

```bash
# Анализ калибровочных кадров
python3 evaluate_frame_differences.py ../data/img_calibration/*.jpg > calibration_report.txt

# Анализ кадров с прямой линии
python3 evaluate_frame_differences.py ../data/img_straight/*.jpg > straight_report.txt
```

### Интеграция в тесты

Скрипт можно использовать в автоматических тестах качества:

```python
from evaluate_frame_differences import analyze_multiple_frames

# Проверка стабильности камеры
image_paths = ["frame1.jpg", "frame2.jpg", "frame3.jpg"]
analyze_multiple_frames(image_paths)
```

## ❓ Частые вопросы

**В: Какое значение SSIM считается нормальным для камеры ESP32CAM?**

О: Для ESP32CAM при съемке однородной поверхности нормальный SSIM > 0.85. Значения 0.90-0.95 указывают на отличную стабильность.

**В: Почему MAE может быть высоким даже при хорошем SSIM?**

О: MAE чувствителен к абсолютным различиям в яркости (например, изменение освещения), в то время как SSIM оценивает структурное сходство. Кадры могут быть структурно похожи, но отличаться по общей яркости.

**В: Сколько кадров нужно для качественного анализа?**

О: Минимум 5-10 кадров для базовой оценки. Для надежной статистики рекомендуется 15-20 кадров.

**В: Можно ли использовать этот скрипт для других типов изображений?**

О: Да, скрипт универсален и работает с любыми grayscale или RGB изображениями. RGB автоматически конвертируется в grayscale.

## 🤝 Связь с другими скриптами

Этот скрипт дополняет другие инструменты анализа:

- `test_line_detection.py` - детекция линии
- `compare_camera_heights.py` - сравнение высоты камеры
- `resolution_comparison.py` - анализ разрешения
- `benchmark_algorithm.py` - тестирование производительности

Вместе они образуют полный набор для тестирования и калибровки камеры робота.
